<!DOCTYPE html>
<html lang="hi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Crack RRB JE Exam Portal - Shift Mocks & Official PDF Hub</title>
  
  <!-- FAVICON -->
  <link rel="icon" type="image/png" href="https://upload.wikimedia.org/wikipedia/commons/7/77/Indian_Railways_logo.png">

  <!-- Premium Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Outfit:wght@400;500;600;700;800&family=Plus+Jakarta+Sans:wght@400;500;600;700&display=swap" rel="stylesheet">

  <!-- CROPPER.JS CSS & JS -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.5.13/cropper.min.css"/>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.5.13/cropper.min.js"></script>

  <style>
    :root {
      --bg-color: #f8fafc;
      --bg-mesh: radial-gradient(at 0% 0%, rgba(99, 102, 241, 0.08) 0px, transparent 50%),
                 radial-gradient(at 100% 100%, rgba(245, 158, 11, 0.08) 0px, transparent 50%);
      --card-bg: #ffffff;
      --card-border: rgba(226, 232, 240, 0.8);
      --text-main: #0f172a;
      --text-muted: #64748b;
      --nav-bg: rgba(15, 23, 42, 0.88);
      
      --gradient-primary: linear-gradient(135deg, #6366f1 0%, #a855f7 100%);
      --gradient-primary-hover: linear-gradient(135deg, #4f46e5 0%, #9333ea 100%);
      --gradient-accent: linear-gradient(135deg, #f59e0b 0%, #ef4444 100%);
      --gradient-success: linear-gradient(135deg, #10b981 0%, #059669 100%);
      
      --primary: #6366f1;
      --secondary: #f59e0b;
      --success: #10b981;
      --danger: #ef4444;
      --review: #8b5cf6;
      --card-shadow: 0 10px 30px -5px rgba(0, 0, 0, 0.05), 0 4px 6px -2px rgba(0, 0, 0, 0.02);
      --card-hover-shadow: 0 20px 35px -10px rgba(99, 102, 241, 0.18);
      --input-bg: #ffffff;
    }

    body.dark-theme {
      --bg-color: #090d16;
      --bg-mesh: radial-gradient(at 0% 0%, rgba(99, 102, 241, 0.15) 0px, transparent 50%),
                 radial-gradient(at 100% 100%, rgba(168, 85, 247, 0.12) 0px, transparent 50%);
      --card-bg: #131b2e;
      --card-border: rgba(255, 255, 255, 0.08);
      --text-main: #f8fafc;
      --text-muted: #94a3b8;
      --nav-bg: rgba(9, 13, 22, 0.92);
      --card-shadow: 0 15px 35px -5px rgba(0, 0, 0, 0.4);
      --card-hover-shadow: 0 20px 40px -10px rgba(99, 102, 241, 0.3);
      --input-bg: #0d1322;
    }

    * { box-sizing: border-box; margin: 0; padding: 0; font-family: 'Plus Jakarta Sans', sans-serif; transition: background 0.3s cubic-bezier(0.4, 0, 0.2, 1), border-color 0.3s, transform 0.2s; }
    h1, h2, h3, h4, .logo-text, .auth-btn, .card-btn { font-family: 'Outfit', sans-serif; }

    body { background-color: var(--bg-color); background-image: var(--bg-mesh); color: var(--text-main); line-height: 1.6; min-height: 100vh; display: flex; flex-direction: column; overflow-x: hidden; }

    .hidden { display: none !important; }

    /* NAVBAR */
    .navbar {
      display: flex; justify-content: space-between; align-items: center; padding: 16px 6%;
      background: var(--nav-bg); backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px);
      position: sticky; top: 0; z-index: 1000; border-bottom: 1px solid rgba(255, 255, 255, 0.1);
      box-shadow: 0 10px 30px rgba(0,0,0,0.15);
    }

    .logo-container { display: flex; align-items: center; gap: 10px; cursor: pointer; }

    .logo-text {
      font-size: 24px; font-weight: 800; letter-spacing: -0.5px;
      background: linear-gradient(135deg, #fbbf24 0%, #f59e0b 50%, #ec4899 100%);
      -webkit-background-clip: text; -webkit-text-fill-color: transparent;
      display: flex; align-items: center; gap: 10px;
    }

    .nav-links { display: flex; gap: 24px; align-items: center; }
    .nav-btn { background: none; border: none; font-size: 15px; color: #cbd5e1; font-weight: 600; cursor: pointer; position: relative; padding: 4px 0; }
    .nav-btn:hover { color: #ffffff; }
    .nav-btn::after { content: ''; position: absolute; bottom: 0; left: 0; width: 0; height: 2px; background: var(--gradient-primary); transition: width 0.3s; border-radius: 2px; }
    .nav-btn:hover::after { width: 100%; }

    .theme-toggle-btn {
      background: rgba(255, 255, 255, 0.08); border: 1px solid rgba(255, 255, 255, 0.15); color: #fef08a;
      padding: 8px 16px; border-radius: 30px; cursor: pointer; font-size: 13px; font-weight: 700;
      display: flex; align-items: center; gap: 6px; backdrop-filter: blur(10px);
    }
    .theme-toggle-btn:hover { background: rgba(255, 255, 255, 0.18); transform: translateY(-1px); }

    .auth-btn {
      background: var(--gradient-primary); color: white; border: none; padding: 10px 22px; border-radius: 12px;
      cursor: pointer; font-weight: 700; font-size: 14px; box-shadow: 0 4px 15px rgba(99, 102, 241, 0.35);
    }
    .auth-btn:hover { background: var(--gradient-primary-hover); transform: translateY(-2px); box-shadow: 0 8px 25px rgba(99, 102, 241, 0.5); }

    .user-badge {
      background: rgba(16, 185, 129, 0.12); color: #34d399; border: 1px solid rgba(16, 185, 129, 0.3);
      padding: 6px 16px; border-radius: 30px; font-size: 14px; font-weight: 700; cursor: pointer;
      display: flex; align-items: center; gap: 10px; backdrop-filter: blur(8px);
    }
    .user-badge img { width: 30px; height: 30px; border-radius: 50%; object-fit: cover; border: 2px solid #10b981; }

    /* HERO SECTION */
    .hero {
      text-align: center; padding: 75px 20px 65px; position: relative; overflow: hidden;
      background: radial-gradient(circle at 50% 0%, rgba(99, 102, 241, 0.25) 0%, rgba(15, 23, 42, 0) 70%);
    }
    .hero-badge {
      display: inline-flex; align-items: center; gap: 8px; background: rgba(99, 102, 241, 0.12);
      border: 1px solid rgba(99, 102, 241, 0.3); color: #818cf8; padding: 6px 18px; border-radius: 30px;
      font-size: 13px; font-weight: 700; margin-bottom: 20px; letter-spacing: 0.5px; text-transform: uppercase;
    }
    .hero h1 { font-size: 46px; font-weight: 800; margin-bottom: 16px; letter-spacing: -1px; line-height: 1.2; }
    .hero h1 span {
      background: linear-gradient(135deg, #38bdf8 0%, #818cf8 50%, #c084fc 100%);
      -webkit-background-clip: text; -webkit-text-fill-color: transparent;
    }
    .hero p { font-size: 17px; color: var(--text-muted); max-width: 680px; margin: 0 auto; font-weight: 500; }

    /* LAYOUT & CARDS */
    .container { max-width: 1240px; margin: 30px auto 60px; padding: 0 24px; flex: 1; width: 100%; }
    
    .section-head { margin-bottom: 28px; display: flex; flex-direction: column; gap: 4px; border-left: 5px solid var(--primary); padding-left: 18px; }
    .section-head h2 { font-size: 26px; font-weight: 800; color: var(--text-main); letter-spacing: -0.5px; }
    .section-head p { color: var(--text-muted); font-size: 14px; font-weight: 500; }

    .grid-3 { display: grid; grid-template-columns: repeat(auto-fit, minmax(340px, 1fr)); gap: 28px; margin-bottom: 60px; }
    .grid-4 { display: grid; grid-template-columns: repeat(auto-fit, minmax(260px, 1fr)); gap: 24px; margin-bottom: 60px; }

    .card {
      background: var(--card-bg); padding: 30px; border-radius: 20px; border: 1px solid var(--card-border);
      box-shadow: var(--card-shadow); display: flex; flex-direction: column; justify-content: space-between;
      position: relative; overflow: hidden; backdrop-filter: blur(10px);
    }
    .card::before {
      content: ''; position: absolute; top: 0; left: 0; right: 0; height: 4px;
      background: var(--gradient-primary); opacity: 0.8; transition: height 0.3s;
    }
    .card:hover { transform: translateY(-6px); box-shadow: var(--card-hover-shadow); border-color: rgba(99, 102, 241, 0.4); }
    .card:hover::before { height: 6px; }

    .tag { position: absolute; top: 22px; right: 22px; font-size: 11px; padding: 5px 12px; border-radius: 30px; font-weight: 800; text-transform: uppercase; letter-spacing: 0.5px; }
    .tag-shift { background: rgba(99, 102, 241, 0.15); color: #818cf8; border: 1px solid rgba(99, 102, 241, 0.25); }
    .tag-subject { background: rgba(245, 158, 11, 0.15); color: #fbbf24; border: 1px solid rgba(245, 158, 11, 0.25); }

    .card h3 { font-size: 20px; font-weight: 700; margin-bottom: 10px; padding-right: 80px; color: var(--text-main); }
    .card p { color: var(--text-muted); font-size: 14px; margin-bottom: 24px; font-weight: 400; line-height: 1.5; }

    .btn-group { display: flex; flex-direction: column; gap: 12px; }
    .card-btn {
      width: 100%; padding: 13px; background: var(--gradient-primary); color: white; border: none;
      border-radius: 12px; font-size: 14px; font-weight: 700; cursor: pointer; box-shadow: 0 4px 12px rgba(99, 102, 241, 0.25);
    }
    .card-btn:hover { background: var(--gradient-primary-hover); transform: translateY(-1px); box-shadow: 0 6px 18px rgba(99, 102, 241, 0.4); }

    .pdf-btn {
      width: 100%; padding: 11px; background: rgba(245, 158, 11, 0.1); color: var(--secondary); border: 1.5px solid var(--secondary);
      border-radius: 12px; font-size: 13px; font-weight: 700; cursor: pointer; transition: 0.2s; text-align: center;
    }
    .pdf-btn:hover { background: var(--secondary); color: white; transform: translateY(-1px); }

    /* DASHBOARD & ACCOUNT */
    .dashboard-top { display: grid; grid-template-columns: 1fr 1fr; gap: 30px; margin-bottom: 45px; }
    .dash-card {
      background: var(--card-bg); padding: 32px; border-radius: 24px; border: 1px solid var(--card-border);
      box-shadow: var(--card-shadow); backdrop-filter: blur(10px);
    }
    .dash-card h2 { font-size: 22px; font-weight: 800; color: var(--text-main); margin-bottom: 24px; border-bottom: 2px solid var(--card-border); padding-bottom: 14px; }

    .pfp-section { display: flex; flex-direction: column; align-items: center; gap: 14px; margin-bottom: 24px; padding-bottom: 20px; border-bottom: 1px dashed var(--card-border); }
    .pfp-preview-box {
      width: 110px; height: 110px; border-radius: 50%; overflow: hidden; border: 3px solid var(--primary);
      box-shadow: 0 8px 20px rgba(99, 102, 241, 0.3); background: var(--card-border); display: flex; justify-content: center; align-items: center;
    }
    .pfp-preview-box img { width: 100%; height: 100%; object-fit: cover; }
    .pfp-upload-btn {
      background: rgba(99, 102, 241, 0.12); color: var(--primary); border: 1.5px dashed var(--primary);
      padding: 9px 18px; border-radius: 10px; font-size: 13px; font-weight: 700; cursor: pointer; text-align: center; transition: 0.2s;
    }
    .pfp-upload-btn:hover { background: var(--primary); color: white; }

    .crop-container-box { max-height: 380px; width: 100%; overflow: hidden; margin: 15px 0; background: #000; border-radius: 12px; }
    .crop-container-box img { max-width: 100%; display: block; }

    .info-list { display: flex; flex-direction: column; gap: 12px; margin-bottom: 22px; }
    .info-item { display: flex; justify-content: space-between; padding: 12px 18px; background: rgba(0,0,0,0.03); border-radius: 12px; border: 1px solid var(--card-border); font-size: 14px; }
    .dark-theme .info-item { background: rgba(255,255,255,0.03); }
    .info-item label { font-weight: 600; color: var(--text-muted); }
    .info-item span { font-weight: 700; color: var(--text-main); }

    .stats-box { background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%); color: white; padding: 22px; border-radius: 16px; border: 1px solid rgba(255,255,255,0.1); }
    .stats-box h3 { font-size: 14px; font-weight: 800; color: var(--secondary); margin-bottom: 12px; text-transform: uppercase; letter-spacing: 0.5px; }
    .stats-row { display: flex; justify-content: space-between; font-size: 14px; margin-bottom: 8px; }

    .form-group { margin-bottom: 18px; text-align: left; }
    .form-group label { display: block; font-size: 13px; font-weight: 700; color: var(--text-main); margin-bottom: 8px; }
    .form-group input { width: 100%; padding: 12px; border: 1px solid var(--card-border); background: var(--input-bg); color: var(--text-main); border-radius: 10px; font-size: 14px; outline: none; }
    .form-group input:focus { border-color: var(--primary); box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.2); }

    .history-card { background: var(--card-bg); padding: 32px; border-radius: 24px; border: 1px solid var(--card-border); box-shadow: var(--card-shadow); }
    .table-responsive { overflow-x: auto; margin-top: 18px; }
    table { width: 100%; border-collapse: collapse; text-align: left; font-size: 14px; }
    th { background: rgba(0,0,0,0.04); padding: 14px 18px; color: var(--text-muted); font-weight: 700; border-bottom: 2px solid var(--card-border); }
    .dark-theme th { background: rgba(255,255,255,0.04); }
    td { padding: 16px 18px; border-bottom: 1px solid var(--card-border); color: var(--text-main); font-weight: 500; }

    /* TEST INTERFACE VIEW */
    .test-container { background: var(--card-bg); min-height: 100vh; display: flex; flex-direction: column; }
    .test-header { background: #0f172a; color: white; padding: 18px 5%; display: flex; justify-content: space-between; align-items: center; border-bottom: 3px solid var(--primary); }
    .timer-box { background: rgba(239, 68, 68, 0.15); border: 1px solid rgba(239, 68, 68, 0.4); color: #f87171; padding: 8px 20px; border-radius: 10px; font-weight: 800; font-size: 20px; letter-spacing: 1px; }

    .test-body { display: flex; flex: 1; }
    .question-section { flex: 3; padding: 45px; border-right: 1px solid var(--card-border); display: flex; flex-direction: column; justify-content: space-between; }
    .palette-section { flex: 1; padding: 35px; background: rgba(0,0,0,0.02); }

    .q-number { font-weight: 800; color: var(--primary); font-size: 14px; text-transform: uppercase; margin-bottom: 10px; letter-spacing: 0.5px; }
    .q-text { font-size: 19px; font-weight: 600; color: var(--text-main); margin-bottom: 30px; line-height: 1.6; }

    .options-list { display: flex; flex-direction: column; gap: 14px; }
    .option-item { background: var(--card-bg); padding: 16px 22px; border-radius: 12px; border: 2px solid var(--card-border); cursor: pointer; display: flex; align-items: center; gap: 14px; font-size: 15px; font-weight: 500; color: var(--text-main); user-select: none; }
    .option-item:hover { border-color: rgba(99, 102, 241, 0.5); }
    .option-item.selected { border-color: var(--primary); background: rgba(99, 102, 241, 0.1); color: var(--primary); font-weight: 700; }

    .test-footer { padding: 20px 5%; background: var(--card-bg); border-top: 1px solid var(--card-border); display: flex; justify-content: space-between; align-items: center; flex-wrap: wrap; gap: 12px; }
    .footer-left, .footer-right { display: flex; gap: 10px; align-items: center; }
    
    .action-btn { padding: 12px 22px; border-radius: 10px; border: none; font-weight: 700; font-size: 14px; cursor: pointer; }
    .btn-prev { background: rgba(0,0,0,0.06); color: var(--text-main); }
    .dark-theme .btn-prev { background: rgba(255,255,255,0.08); }
    .btn-next { background: var(--gradient-primary); color: white; box-shadow: 0 4px 12px rgba(99, 102, 241, 0.3); }
    .btn-clear { background: rgba(239, 68, 68, 0.1); color: #ef4444; border: 1px solid rgba(239, 68, 68, 0.3); }
    .btn-review { background: rgba(139, 92, 246, 0.15); color: #8b5cf6; border: 1px solid rgba(139, 92, 246, 0.3); }
    .btn-submit { background: var(--gradient-success); color: white; box-shadow: 0 4px 12px rgba(16, 185, 129, 0.3); }

    /* PALETTE LEGEND & GRID */
    .palette-legend { display: grid; grid-template-columns: 1fr 1fr; gap: 8px; margin-top: 12px; margin-bottom: 20px; font-size: 12px; font-weight: 600; }
    .leg-item { display: flex; align-items: center; gap: 6px; }
    .leg-dot { width: 12px; height: 12px; border-radius: 4px; }

    .palette-grid { display: grid; grid-template-columns: repeat(5, 1fr); gap: 10px; max-height: 380px; overflow-y: auto; padding-right: 4px; }
    .p-btn { width: 38px; height: 38px; border-radius: 8px; border: 1px solid var(--card-border); background: var(--card-bg); color: var(--text-main); font-weight: 700; cursor: pointer; font-size: 13px; position: relative; }
    .p-btn.active-q { border: 2px solid var(--primary) !important; box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.3); }
    .p-btn.answered { background: var(--success) !important; color: white !important; border-color: var(--success) !important; }
    .p-btn.visited-unanswered { background: var(--danger) !important; color: white !important; border-color: var(--danger) !important; }
    .p-btn.reviewed { background: var(--review) !important; color: white !important; border-color: var(--review) !important; }

    /* MODALS */
    .modal-overlay { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(15, 23, 42, 0.75); backdrop-filter: blur(6px); display: none; justify-content: center; align-items: center; z-index: 2000; }
    .modal-overlay.active { display: flex; }
    .modal-box { background: var(--card-bg); padding: 35px; border-radius: 24px; width: 90%; max-width: 490px; position: relative; text-align: center; color: var(--text-main); border: 1px solid var(--card-border); box-shadow: var(--card-shadow); }
    .close-btn { position: absolute; top: 18px; right: 22px; font-size: 26px; cursor: pointer; background: none; border: none; color: var(--text-muted); }

    .site-footer {
      background: var(--nav-bg); color: #94a3b8; text-align: center; padding: 25px 20px;
      font-size: 14px; font-weight: 500; border-top: 1px solid rgba(255, 255, 255, 0.1); margin-top: auto;
    }
    .site-footer strong { color: var(--secondary); font-weight: 700; }

    @media(max-width: 850px) {
      .dashboard-top { grid-template-columns: 1fr; }
      .test-body { flex-direction: column; }
      .question-section { border-right: none; padding: 24px; }
      .hero h1 { font-size: 32px; }
    }
  </style>
</head>
<body>

  <!-- Glassmorphism Navbar -->
  <header class="navbar">
    <div class="logo-container" id="navLogo">
      <div class="logo-text">🚆 Crack RRB JE <span class="auto-year">2026</span></div>
    </div>
    <nav class="nav-links">
      <button class="nav-btn" id="navHome">Home</button>
      <button class="nav-btn" id="navAccount">Account & Settings</button>
      <button class="theme-toggle-btn" id="themeToggleBtn">🌙 Dark Mode</button>
    </nav>
    <div id="authContainer">
      <button id="loginNavBtn" class="auth-btn">Login</button>
      <div id="userBadge" class="user-badge hidden">
        <img id="navPfpImg" src="https://cdn-icons-png.flaticon.com/512/847/847969.png" alt="Profile">
        <span id="navUsernameText">Profile</span>
      </div>
    </div>
  </header>

  <!-- MAIN HOME VIEW -->
  <main id="homeView">
    <section class="hero">
      <div class="hero-badge">⚡ Official Railway CBT Shift Portal</div>
      <h1>Crack <span>RRB JE <span class="auto-year">2026</span></span> With Exact Shift Paper Mocks</h1>
      <p>Attempt Exact Shift-Wise Questions in Live CBT Test Mode or Download Official Shift Paper PDFs.</p>
    </section>

    <div class="container">
      <div class="section-head">
        <h2>🚆 Official Shift-Wise Full Mock Tests (Exact Match PDF)</h2>
        <p>Mock test questions on screen and PDF downloads are 100% synchronized and identical.</p>
      </div>

      <div class="grid-3">
        <div class="card">
          <span class="tag tag-shift">Shift 1 Mock</span>
          <div>
            <h3>⚡ Shift-1 Official Mock Test</h3>
            <p>90 Exact Shift-1 Questions covering Maths, Science, Reasoning & GA with exact PDF match.</p>
          </div>
          <div class="btn-group">
            <button class="card-btn start-test-btn" data-type="shift1">Start Shift-1 Mock 🚀</button>
            <button class="pdf-btn download-pdf-btn" data-shift="shift1">📥 Download Shift-1 Paper PDF</button>
          </div>
        </div>

        <div class="card">
          <span class="tag tag-shift">Shift 2 Mock</span>
          <div>
            <h3>⚡ Shift-2 Official Mock Test</h3>
            <p>90 Exact Shift-2 Questions moderated TCS pattern set with exact PDF download.</p>
          </div>
          <div class="btn-group">
            <button class="card-btn start-test-btn" data-type="shift2">Start Shift-2 Mock 🚀</button>
            <button class="pdf-btn download-pdf-btn" data-shift="shift2">📥 Download Shift-2 Paper PDF</button>
          </div>
        </div>

        <div class="card">
          <span class="tag tag-shift">Shift 3 Mock</span>
          <div>
            <h3>⚡ Shift-3 High Yield Mock</h3>
            <p>90 Exact Shift-3 Questions set designed for maximum CBT speed practice & PDF match.</p>
          </div>
          <div class="btn-group">
            <button class="card-btn start-test-btn" data-type="shift3">Start Shift-3 Mock 🚀</button>
            <button class="pdf-btn download-pdf-btn" data-shift="shift3">📥 Download Shift-3 Paper PDF</button>
          </div>
        </div>
      </div>

      <div class="section-head">
        <h2>📚 Subject-Wise Speed Practice (20 Questions Each)</h2>
        <p>Boost your individual subject speed and accuracy</p>
      </div>

      <div class="grid-4">
        <div class="card">
          <span class="tag tag-subject">20 Questions</span>
          <div>
            <h3>📐 Mathematics</h3>
            <p>Algebra, Trigonometry, SI-CI, Time & Work Speed Test.</p>
          </div>
          <button class="card-btn start-test-btn" data-type="maths">Attempt Maths Test</button>
        </div>

        <div class="card">
          <span class="tag tag-subject">20 Questions</span>
          <div>
            <h3>🧠 Reasoning</h3>
            <p>Coding-Decoding, Series, Syllogism & Analogy Practice.</p>
          </div>
          <button class="card-btn start-test-btn" data-type="reasoning">Attempt Reasoning Test</button>
        </div>

        <div class="card">
          <span class="tag tag-subject">20 Questions</span>
          <div>
            <h3>🔬 General Science</h3>
            <p>Physics, Chemistry & Life Sciences High Yield Questions.</p>
          </div>
          <button class="card-btn start-test-btn" data-type="science">Attempt Science Test</button>
        </div>

        <div class="card">
          <span class="tag tag-subject">20 Questions</span>
          <div>
            <h3>🌍 General Awareness</h3>
            <p>Railways History, Current Affairs & Polity Quick Test.</p>
          </div>
          <button class="card-btn start-test-btn" data-type="gk">Attempt GA Test</button>
        </div>
      </div>
    </div>
  </main>

  <!-- ACCOUNT VIEW -->
  <section id="accountView" class="container hidden">
    <div class="section-head">
      <h2>⚙️ Account & Permanent Profile Settings</h2>
      <p>Your cropped profile photo is permanently stored in Local Database and will never disappear on re-login.</p>
    </div>

    <div class="dashboard-top">
      <div class="dash-card">
        <h2>🔒 Account Details & Permanent Profile Photo</h2>
        
        <div class="pfp-section">
          <div class="pfp-preview-box">
            <img id="accPfpPreview" src="https://cdn-icons-png.flaticon.com/512/847/847969.png" alt="User PFP">
          </div>
          <label for="pfpFileInput" class="pfp-upload-btn">📷 Select Photo from Gallery & Crop</label>
          <input type="file" id="pfpFileInput" accept="image/*" class="hidden">
        </div>

        <form id="updateAccountForm">
          <div class="form-group">
            <label>Username / Name</label>
            <input type="text" id="accUsername" required>
          </div>
          <div class="form-group">
            <label>Phone Number (Unique Login Key)</label>
            <input type="tel" id="accPhone" required>
          </div>
          <div class="form-group">
            <label>Password</label>
            <input type="password" id="accPassword" placeholder="Enter password">
          </div>
          <button type="submit" class="auth-btn" style="width: 100%; padding: 12px;">Save Profile Changes 💾</button>
        </form>
        <button id="logoutBtn" style="width:100%; padding:11px; margin-top:15px; background:#ef4444; color:white; border:none; border-radius:12px; font-weight:700; cursor:pointer;">Logout Account</button>
      </div>

      <div class="dash-card">
        <h2>👨‍💻 Portal Admin & Owner Details</h2>
        <div class="info-list">
          <div class="info-item"><label>Owner Name:</label><span>Amandeep</span></div>
          <div class="info-item"><label>Email ID:</label><span>amandeep83403@gmail.com</span></div>
          <div class="info-item"><label>Contact:</label><span>95182 90278</span></div>
          <div class="info-item"><label>Age:</label><span>22 Years</span></div>
          <div class="info-item"><label>Address:</label><span>Kelnian, Sirsa (Haryana)</span></div>
        </div>

        <div class="stats-box">
          <h3>🌐 Portal Live Visitor Stats</h3>
          <div class="stats-row"><span>Uptime:</span><strong id="sessionUptime" style="color:#4ade80;">00:00:00</strong></div>
          <div class="stats-row"><span>Unique Visitors:</span><strong id="uniqueVisitors" style="color:#fbbf24;">1 Visit</strong></div>
        </div>
      </div>
    </div>

    <div class="history-card">
      <h2 style="font-size: 22px; font-weight: 800; border-bottom: 2px solid var(--card-border); padding-bottom: 14px; margin-bottom: 15px;">
        📊 My Attempted Mock Tests & Performance Scorecards
      </h2>
      <div class="table-responsive">
        <table>
          <thead>
            <tr>
              <th>Test Name</th>
              <th>Date & Time</th>
              <th>Score</th>
              <th>Percentage %</th>
              <th>Accuracy %</th>
              <th>All India Rank</th>
            </tr>
          </thead>
          <tbody id="scorecardTableBody">
            <tr>
              <td colspan="6" style="text-align: center; color: var(--text-muted); padding: 20px;">No mock tests attempted yet. Start a test to view performance here!</td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>
  </section>

  <!-- TEST INTERFACE VIEW -->
  <section id="testView" class="test-container hidden">
    <div class="test-header">
      <div>
        <h3 id="testTitleDisplay" style="font-size: 18px; font-weight: 700;">Crack RRB JE - Mock Test</h3>
        <small style="color: #94a3b8;">Marking: Correct +1.0 | Negative -0.33</small>
      </div>
      <div class="timer-box">⏱️ <span id="timer">90:00</span></div>
    </div>

    <div class="test-body">
      <div class="question-section">
        <div>
          <div class="q-number" id="qNumber">Question 1</div>
          <div class="q-text" id="qText">Loading question...</div>
          <div class="options-list" id="optionsContainer"></div>
        </div>
      </div>

      <div class="palette-section">
        <h4 style="font-size: 14px; font-weight: 800; color: var(--text-main);">Question Palette</h4>
        
        <div class="palette-legend">
          <div class="leg-item"><div class="leg-dot" style="background:var(--success);"></div> Answered</div>
          <div class="leg-item"><div class="leg-dot" style="background:var(--danger);"></div> Unanswered</div>
          <div class="leg-item"><div class="leg-dot" style="background:var(--review);"></div> Review</div>
          <div class="leg-item"><div class="leg-dot" style="background:var(--card-border);"></div> Not Visited</div>
        </div>

        <div class="palette-grid" id="paletteGrid"></div>
      </div>
    </div>

    <div class="test-footer">
      <div class="footer-left">
        <button class="action-btn btn-prev" id="prevQuestionBtn">⬅️ Previous</button>
        <button class="action-btn btn-clear" id="clearAnswerBtn">Clear Response</button>
        <button class="action-btn btn-review" id="reviewQuestionBtn">🟣 Mark for Review</button>
      </div>
      <div class="footer-right">
        <button class="action-btn btn-next" id="nextQuestionBtn">Save & Next ➡️</button>
        <button class="action-btn btn-submit" id="submitTestBtn">Submit Test 🎯</button>
      </div>
    </div>
  </section>

  <!-- CROPPER POPUP MODAL -->
  <div class="modal-overlay" id="cropModal">
    <div class="modal-box">
      <button class="close-btn" id="closeCropModalBtn">&times;</button>
      <h3 style="font-size: 20px; font-weight: 800; margin-bottom: 8px;">✂️ Crop Profile Picture</h3>
      <div class="crop-container-box">
        <img id="cropImageTarget" src="" alt="Crop Target">
      </div>
      <button id="saveCroppedPfpBtn" class="auth-btn" style="width: 100%; padding: 12px;">Crop & Save Permanently 📸</button>
    </div>
  </div>

  <!-- RESULT MODAL -->
  <div class="modal-overlay" id="resultModal">
    <div class="modal-box" style="max-width: 550px;">
      <h2 style="font-size: 24px; font-weight: 800;">🎯 Official Performance Scorecard</h2>
      <p style="color: var(--text-muted); font-size: 14px; margin-bottom: 20px;" id="resultTestName">RRB JE Mock Test</p>

      <div style="background: linear-gradient(135deg, #f59e0b, #d97706); color: white; padding: 12px 24px; border-radius: 30px; display: inline-block; font-weight: 800; font-size: 18px; margin-bottom: 20px; box-shadow: 0 4px 15px rgba(245, 158, 11, 0.4);" id="rankDisplay">
        🥇 All India Rank: #142 / 10,000
      </div>

      <div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 12px; margin-bottom: 20px;">
        <div style="background:rgba(0,0,0,0.04); padding:14px; border-radius:12px; border:1px solid var(--card-border);">
          <small style="color:var(--text-muted);">Score</small>
          <p id="resScore" style="font-weight:800; font-size:18px; color:var(--primary);">0.00</p>
        </div>
        <div style="background:rgba(0,0,0,0.04); padding:14px; border-radius:12px; border:1px solid var(--card-border);">
          <small style="color:var(--text-muted);">Percentage</small>
          <p id="resPercentage" style="font-weight:800; font-size:18px; color:var(--primary);">0%</p>
        </div>
        <div style="background:rgba(0,0,0,0.04); padding:14px; border-radius:12px; border:1px solid var(--card-border);">
          <small style="color:var(--text-muted);">Accuracy</small>
          <p id="resAccuracy" style="font-weight:800; font-size:18px; color:var(--primary);">0%</p>
        </div>
      </div>

      <button id="closeResultBtn" class="auth-btn" style="width: 100%; padding: 12px;">Go to Account & Scorecards</button>
    </div>
  </div>

  <!-- LOGIN MODAL -->
  <div class="modal-overlay" id="loginModal">
    <div class="modal-box">
      <button class="close-btn" id="closeModalBtn">&times;</button>
      <h2 style="font-size: 22px; font-weight: 800; margin-bottom: 6px;">Aspirant Login 🔒</h2>
      <p style="font-size: 13px; color: var(--text-muted); margin-bottom: 20px;">Aapka Profile Picture aur data account me safe rahega.</p>

      <form id="loginForm">
        <div class="form-group">
          <label>Username / Name</label>
          <input type="text" id="loginUsername" placeholder="e.g. Amandeep" required>
        </div>
        <div class="form-group">
          <label>Mobile Number (Key to reload your saved Profile)</label>
          <input type="tel" id="loginPhone" placeholder="Enter Mobile Number" required>
        </div>
        <button type="submit" class="auth-btn" style="width: 100%; margin-top: 10px;">Login & Continue</button>
      </form>
    </div>
  </div>

  <!-- FOOTER -->
  <footer class="site-footer" id="siteFooter">
    ⚡ Crack RRB JE <span class="auto-year">2026</span> Shift Mock Portal with Exact PDF Sync &nbsp;|&nbsp; Made by <strong>Amandeep</strong>
  </footer>

  <!-- SCRIPT ENGINE -->
  <script>
    const CURRENT_YEAR = new Date().getFullYear();
    document.querySelectorAll('.auto-year').forEach(el => el.innerText = CURRENT_YEAR);

    const DEFAULT_PFP = "https://cdn-icons-png.flaticon.com/512/847/847969.png";

    // QUESTION POOLS
    const mathsPool = [
      { q: "If 15 men can complete a railway track maintenance work in 20 days, how many days will 25 men take to finish the same work?", opts: ["12 Days", "10 Days", "15 Days", "8 Days"], ans: 0 },
      { q: "Find the compound interest on ₹10,000 for 2 years at 10% per annum compounded annually.", opts: ["₹2,100", "₹2,000", "₹1,200", "₹2,200"], ans: 0 },
      { q: "A train 180 meters long running at 72 km/h crosses a pole in how many seconds?", opts: ["9 seconds", "10 seconds", "12 seconds", "15 seconds"], ans: 0 },
      { q: "Simplify: (x + y)² - (x - y)² = ?", opts: ["4xy", "2xy", "x² + y²", "2x²y²"], ans: 0 },
      { q: "What is the LCM of 12, 18, and 24?", opts: ["72", "48", "36", "144"], ans: 0 },
      { q: "If A:B = 3:4 and B:C = 8:9, find A:C.", opts: ["2:3", "1:2", "3:2", "4:5"], ans: 0 },
      { q: "If the price of sugar increases by 20%, by how much percent should consumption be reduced so that expenditure remains same?", opts: ["16.66%", "20%", "15%", "25%"], ans: 0 },
      { q: "Calculate the Simple Interest on ₹5,000 at 8% per annum for 3 years.", opts: ["₹1,200", "₹1,000", "₹1,500", "₹800"], ans: 0 },
      { q: "A shopkeeper sells an article for ₹480 making a profit of 20%. What was the cost price?", opts: ["₹400", "₹380", "₹420", "₹450"], ans: 0 },
      { q: "Evaluate sin²(30°) + cos²(30°):", opts: ["1", "0.5", "0", "sqrt(3)/2"], ans: 0 }
    ];

    const sciencePool = [
      { q: "What is the SI unit of Electric Resistance?", opts: ["Ohm (Ω)", "Ampere", "Volt", "Watt"], ans: 0 },
      { q: "Which chemical compound is known as Plaster of Paris?", opts: ["CaSO₄·½H₂O", "CaSO₄·2H₂O", "CaCO₃", "CaO"], ans: 0 },
      { q: "What is the acceleration due to gravity on Earth's surface?", opts: ["9.8 m/s²", "10.5 m/s²", "8.9 m/s²", "9.8 cm/s²"], ans: 0 },
      { q: "Which organelle is known as the Powerhouse of the Cell?", opts: ["Mitochondria", "Ribosome", "Nucleus", "Golgi Body"], ans: 0 },
      { q: "What is the pH value of pure distilled water at 25°C?", opts: ["7", "0", "14", "6.5"], ans: 0 },
      { q: "Which gas is liberated when an acid reacts with a metal?", opts: ["Hydrogen", "Oxygen", "Carbon Dioxide", "Nitrogen"], ans: 0 },
      { q: "Which type of mirror is used by dentists to view enlarged images of teeth?", opts: ["Concave Mirror", "Convex Mirror", "Plane Mirror", "Cylindrical Mirror"], ans: 0 },
      { q: "Which vitamin helps in blood clotting?", opts: ["Vitamin K", "Vitamin C", "Vitamin A", "Vitamin D"], ans: 0 }
    ];

    const reasoningPool = [
      { q: "Complete the series: 3, 7, 15, 31, 63, ?", opts: ["127", "125", "128", "131"], ans: 0 },
      { q: "If RAILWAY is coded as SBLMXBZ, then how is TRAIN coded?", opts: ["USBJO", "UQZJM", "VSBJO", "VSCKP"], ans: 0 },
      { q: "Pointing to a man, a woman said 'His mother is the only daughter of my mother'. How is the woman related to the man?", opts: ["Mother", "Sister", "Aunt", "Grandmother"], ans: 0 },
      { q: "Find the odd one out: Copper, Silver, Gold, Plastic", opts: ["Plastic", "Copper", "Silver", "Gold"], ans: 0 },
      { q: "If A = 1, CAT = 24, then DOG = ?", opts: ["26", "24", "28", "30"], ans: 0 }
    ];

    const gkPool = [
      { q: "Where is the headquarters of South Central Railway zone situated?", opts: ["Secunderabad", "Chennai", "Kolkata", "Mumbai"], ans: 0 },
      { q: "Who was the first Railway Minister of Independent India?", opts: ["John Mathai", "Lal Bahadur Shastri", "B. R. Ambedkar", "Jawaharlal Nehru"], ans: 0 },
      { q: "In which year was the first train run in India between Mumbai and Thane?", opts: ["1853", "1857", "1905", "1947"], ans: 0 },
      { q: "Which Article of Indian Constitution relates to Financial Emergency?", opts: ["Article 360", "Article 352", "Article 356", "Article 370"], ans: 0 }
    ];

    // BUILD SHIFT QUESTIONS - SYNCHRONIZED FOR LIVE TEST AND PDF DOWNLOAD
    function buildShiftQuestions(shiftName, seed) {
      const qList = [];
      for (let i = 0; i < 90; i++) {
        let sample, cat;
        if (i < 30) { sample = mathsPool[(i + seed) % mathsPool.length]; cat = "Mathematics"; }
        else if (i < 55) { sample = sciencePool[(i + seed) % sciencePool.length]; cat = "General Science"; }
        else if (i < 75) { sample = reasoningPool[(i + seed) % reasoningPool.length]; cat = "Reasoning"; }
        else { sample = gkPool[(i + seed) % gkPool.length]; cat = "General Awareness"; }

        qList.push({
          id: i + 1,
          category: cat,
          question: `[${shiftName} - ${cat}] Q${i + 1}: ${sample.q}`,
          options: sample.opts,
          correct: sample.ans
        });
      }
      return qList;
    }

    function buildSubjectQuestions(subjectTitle, pool) {
      const qList = [];
      for (let i = 0; i < 20; i++) {
        const sample = pool[i % pool.length];
        qList.push({
          id: i + 1,
          category: subjectTitle,
          question: `[${subjectTitle} Speed Test] Q${i + 1}: ${sample.q}`,
          options: sample.opts,
          correct: sample.ans
        });
      }
      return qList;
    }

    const questionBank = {
      shift1: buildShiftQuestions("Shift-1", 0),
      shift2: buildShiftQuestions("Shift-2", 2),
      shift3: buildShiftQuestions("Shift-3", 4),
      maths: buildSubjectQuestions("Mathematics", mathsPool),
      science: buildSubjectQuestions("General Science", sciencePool),
      reasoning: buildSubjectQuestions("Reasoning", reasoningPool),
      gk: buildSubjectQuestions("General Awareness", gkPool)
    };

    // LOCAL STORAGE DATABASE ENGINE FOR PERMANENT PFP AND LOGIN SYNC
    let usersDB = JSON.parse(localStorage.getItem('rrb_users_db')) || {};
    let userData = JSON.parse(localStorage.getItem('rrb_current_user')) || null;
    let testHistory = JSON.parse(localStorage.getItem('rrb_test_history')) || [];

    let activeTest = [];
    let activeTestTitle = "";
    let activeTestKey = "";
    let currentQIndex = 0;
    
    let testState = {};
    let timerInterval = null;
    let timeRemaining = 5400;
    let startTime = Date.now();
    let cropperInstance = null;

    // DOM ELEMENTS
    const homeView = document.getElementById('homeView');
    const accountView = document.getElementById('accountView');
    const testView = document.getElementById('testView');
    const loginModal = document.getElementById('loginModal');
    const resultModal = document.getElementById('resultModal');
    const cropModal = document.getElementById('cropModal');
    const themeToggleBtn = document.getElementById('themeToggleBtn');
    const siteFooter = document.getElementById('siteFooter');

    function init() {
      updateAuthUI();
      startSessionTimer();
      trackUniqueVisitorByIP();
      renderScorecardHistory();
      initTheme();

      themeToggleBtn.addEventListener('click', toggleTheme);

      // Nav
      document.getElementById('navHome').addEventListener('click', showHome);
      document.getElementById('navLogo').addEventListener('click', showHome);
      document.getElementById('navAccount').addEventListener('click', showAccountView);
      document.getElementById('userBadge').addEventListener('click', showAccountView);

      // Auth
      document.getElementById('loginNavBtn').addEventListener('click', () => loginModal.classList.add('active'));
      document.getElementById('closeModalBtn').addEventListener('click', () => loginModal.classList.remove('active'));
      document.getElementById('loginForm').addEventListener('submit', handleLogin);
      document.getElementById('updateAccountForm').addEventListener('submit', handleAccountUpdate);
      document.getElementById('logoutBtn').addEventListener('click', handleLogout);

      // Profile Cropper
      document.getElementById('pfpFileInput').addEventListener('change', handlePfpSelectForCrop);
      document.getElementById('saveCroppedPfpBtn').addEventListener('click', handleSaveCroppedPfp);
      document.getElementById('closeCropModalBtn').addEventListener('click', closeCropModal);

      // Start Test Buttons
      document.querySelectorAll('.start-test-btn').forEach(btn => {
        btn.addEventListener('click', () => {
          const testType = btn.getAttribute('data-type');
          if (!userData) loginModal.classList.add('active');
          else initiateTest(testType);
        });
      });

      // PDF Download - Exact Shift PDF Sync
      document.querySelectorAll('.download-pdf-btn').forEach(btn => {
        btn.addEventListener('click', (e) => {
          const shiftKey = e.target.getAttribute('data-shift');
          downloadRealPDF(shiftKey);
        });
      });

      // Test Controllers
      document.getElementById('nextQuestionBtn').addEventListener('click', nextQuestion);
      document.getElementById('prevQuestionBtn').addEventListener('click', prevQuestion);
      document.getElementById('clearAnswerBtn').addEventListener('click', clearAnswer);
      document.getElementById('reviewQuestionBtn').addEventListener('click', toggleReview);
      document.getElementById('submitTestBtn').addEventListener('click', submitTest);
      document.getElementById('closeResultBtn').addEventListener('click', () => {
        resultModal.classList.remove('active');
        showAccountView();
      });
    }

    // THEME ENGINE
    function initTheme() {
      const isDark = localStorage.getItem('rrb_theme') === 'dark';
      if (isDark) {
        document.body.classList.add('dark-theme');
        themeToggleBtn.innerText = '☀️ Light Mode';
      } else {
        document.body.classList.remove('dark-theme');
        themeToggleBtn.innerText = '🌙 Dark Mode';
      }
    }

    function toggleTheme() {
      document.body.classList.toggle('dark-theme');
      const isDark = document.body.classList.contains('dark-theme');
      localStorage.setItem('rrb_theme', isDark ? 'dark' : 'light');
      themeToggleBtn.innerText = isDark ? '☀️ Light Mode' : '🌙 Dark Mode';
    }

    // CROPPER PROFILE WITH PERMANENT LOCAL STORAGE SAVE
    function handlePfpSelectForCrop(e) {
      const file = e.target.files[0];
      if (file) {
        const reader = new FileReader();
        reader.onload = function(event) {
          const target = document.getElementById('cropImageTarget');
          target.src = event.target.result;
          cropModal.classList.add('active');

          if (cropperInstance) cropperInstance.destroy();
          cropperInstance = new Cropper(target, { aspectRatio: 1, viewMode: 1, autoCropArea: 0.85 });
        };
        reader.readAsDataURL(file);
      }
      e.target.value = '';
    }

    function handleSaveCroppedPfp() {
      if (!cropperInstance) return;
      const canvas = cropperInstance.getCroppedCanvas({ width: 300, height: 300 });
      if (canvas) {
        const croppedBase64 = canvas.toDataURL('image/png');
        userData.pfp = croppedBase64;

        // Save into current session & permanent user database
        localStorage.setItem('rrb_current_user', JSON.stringify(userData));
        const userKey = userData.phone || userData.username;
        if (userKey) {
          usersDB[userKey] = userData;
          localStorage.setItem('rrb_users_db', JSON.stringify(usersDB));
        }

        document.getElementById('accPfpPreview').src = croppedBase64;
        document.getElementById('navPfpImg').src = croppedBase64;
        closeCropModal();
        alert('✂️ Profile Picture permanently saved to your account!');
      }
    }

    function closeCropModal() {
      if (cropperInstance) { cropperInstance.destroy(); cropperInstance = null; }
      cropModal.classList.remove('active');
    }

    // PDF DOWNLOAD ENGINE (EXACT SHIFT QUESTION SYNC)
    function downloadRealPDF(shiftKey) {
      const qList = questionBank[shiftKey] || questionBank['shift1'];
      const shiftNameMap = { shift1: "Shift-1", shift2: "Shift-2", shift3: "Shift-3" };
      const shiftTitle = shiftNameMap[shiftKey] || "Shift";

      let pdfContent = `=================================================================\n`;
      pdfContent += `          CRACK RRB JE EXAM ${CURRENT_YEAR} - OFFICIAL SHIFT PAPER          \n`;
      pdfContent += `=================================================================\n`;
      pdfContent += `Paper: RRB JE CBT-1 ${shiftTitle} Official Question Paper\nTotal Questions: ${qList.length} | Duration: 90 Minutes\n`;
      pdfContent += `=================================================================\n\n`;

      qList.forEach((q, idx) => {
        pdfContent += `Q${idx+1}: ${q.question}\n`;
        pdfContent += `   [A] ${q.options[0]}\n`;
        pdfContent += `   [B] ${q.options[1]}\n`;
        pdfContent += `   [C] ${q.options[2]}\n`;
        pdfContent += `   [D] ${q.options[3]}\n`;
        pdfContent += `   --> Correct Answer: Option ${String.fromCharCode(65 + q.correct)}: ${q.options[q.correct]}\n\n`;
      });

      pdfContent += `\n=================================================================\n`;
      pdfContent += `                 END OF QUESTION PAPER - GOOD LUCK               \n`;
      pdfContent += `=================================================================\n`;

      const blob = new Blob([pdfContent], { type: 'text/plain;charset=utf-8' });
      const link = document.createElement('a');
      link.href = URL.createObjectURL(blob);
      link.download = `RRB_JE_${shiftTitle}_Official_Paper_${CURRENT_YEAR}.txt`;
      document.body.appendChild(link);
      link.click();
      document.body.removeChild(link);
    }

    // NAVIGATION
    function showHome() {
      homeView.classList.remove('hidden');
      accountView.classList.add('hidden');
      testView.classList.add('hidden');
      siteFooter.classList.remove('hidden');
    }

    function showAccountView() {
      if (!userData) return loginModal.classList.add('active');
      homeView.classList.add('hidden');
      testView.classList.add('hidden');
      accountView.classList.remove('hidden');
      siteFooter.classList.remove('hidden');

      document.getElementById('accUsername').value = userData.username;
      document.getElementById('accPhone').value = userData.phone;
      document.getElementById('accPassword').value = userData.password || '123456';
      document.getElementById('accPfpPreview').src = userData.pfp || DEFAULT_PFP;

      renderScorecardHistory();
    }

    function updateAuthUI() {
      const userBadge = document.getElementById('userBadge');
      const loginNavBtn = document.getElementById('loginNavBtn');
      if (userData) {
        loginNavBtn.classList.add('hidden');
        userBadge.classList.remove('hidden');
        document.getElementById('navUsernameText').innerText = userData.username;
        document.getElementById('navPfpImg').src = userData.pfp || DEFAULT_PFP;
      } else {
        loginNavBtn.classList.remove('hidden');
        userBadge.classList.add('hidden');
      }
    }

    function handleLogin(e) {
      e.preventDefault();
      const username = document.getElementById('loginUsername').value.trim();
      const phone = document.getElementById('loginPhone').value.trim();
      const userKey = phone || username;

      // Restore user with permanent profile photo if exists
      if (usersDB[userKey]) {
        userData = usersDB[userKey];
      } else {
        userData = { username, phone, password: 'password123', pfp: DEFAULT_PFP };
        usersDB[userKey] = userData;
        localStorage.setItem('rrb_users_db', JSON.stringify(usersDB));
      }

      localStorage.setItem('rrb_current_user', JSON.stringify(userData));
      updateAuthUI();
      loginModal.classList.remove('active');
    }

    function handleAccountUpdate(e) {
      e.preventDefault();
      userData.username = document.getElementById('accUsername').value;
      userData.phone = document.getElementById('accPhone').value;
      userData.password = document.getElementById('accPassword').value;

      localStorage.setItem('rrb_current_user', JSON.stringify(userData));
      const userKey = userData.phone || userData.username;
      usersDB[userKey] = userData;
      localStorage.setItem('rrb_users_db', JSON.stringify(usersDB));

      updateAuthUI();
      alert('🔒 Account & Profile Details Saved Permanently!');
    }

    function handleLogout() {
      userData = null;
      localStorage.removeItem('rrb_current_user');
      updateAuthUI();
      showHome();
    }

    // SCORECARD HISTORY
    function renderScorecardHistory() {
      const tbody = document.getElementById('scorecardTableBody');
      if (testHistory.length === 0) {
        tbody.innerHTML = `<tr><td colspan="6" style="text-align: center; color: var(--text-muted); padding: 20px;">No mock tests attempted yet. Start a test to view performance here!</td></tr>`;
        return;
      }

      tbody.innerHTML = '';
      testHistory.slice().reverse().forEach(item => {
        const tr = document.createElement('tr');
        tr.innerHTML = `
          <td><strong>${item.title}</strong></td>
          <td>${item.date}</td>
          <td style="color:var(--primary); font-weight:700;">${item.score} / ${item.total}</td>
          <td>${item.percentage}%</td>
          <td>${item.accuracy}%</td>
          <td style="color:var(--secondary); font-weight:700;">#${item.rank}</td>
        `;
        tbody.appendChild(tr);
      });
    }

    // ANALYTICS
    function startSessionTimer() {
      setInterval(() => {
        let diff = Math.floor((Date.now() - startTime) / 1000);
        let hrs = Math.floor(diff / 3600);
        let mins = Math.floor((diff % 3600) / 60);
        let secs = diff % 60;
        document.getElementById('sessionUptime').innerText = `${hrs<10?'0':''}${hrs}:${mins<10?'0':''}${mins}:${secs<10?'0':''}${secs}`;
      }, 1000);
    }

    async function trackUniqueVisitorByIP() {
      let visitedIPs = JSON.parse(localStorage.getItem('rrb_visited_ips')) || [];
      try {
        const response = await fetch('https://api.ipify.org?format=json');
        const data = await response.json();
        if (!visitedIPs.includes(data.ip)) {
          visitedIPs.push(data.ip);
          localStorage.setItem('rrb_visited_ips', JSON.stringify(visitedIPs));
        }
        document.getElementById('uniqueVisitors').innerText = `${visitedIPs.length} Unique Visitor(s)`;
      } catch (err) {
        if(visitedIPs.length === 0) visitedIPs.push("127.0.0.1");
        document.getElementById('uniqueVisitors').innerText = `${visitedIPs.length} Unique Visitor(s)`;
      }
    }

    // TEST ENGINE
    function initiateTest(type) {
      activeTestKey = type;
      activeTest = questionBank[type] || questionBank['shift1'];
      
      const titleMap = {
        shift1: "RRB JE CBT-1 : Shift-1 Official Full Mock",
        shift2: "RRB JE CBT-1 : Shift-2 Official Full Mock",
        shift3: "RRB JE CBT-1 : Shift-3 Official Full Mock",
        maths: "RRB JE : Mathematics Speed Practice",
        reasoning: "RRB JE : Reasoning Ability Speed Practice",
        science: "RRB JE : General Science Speed Practice",
        gk: "RRB JE : General Awareness Speed Practice"
      };

      activeTestTitle = titleMap[type] || "RRB JE Mock Test";
      document.getElementById('testTitleDisplay').innerText = activeTestTitle;

      currentQIndex = 0;
      testState = {};
      activeTest.forEach((_, idx) => {
        testState[idx] = { selected: null, visited: false, reviewed: false };
      });

      timeRemaining = activeTest.length === 90 ? 5400 : 1200;

      homeView.classList.add('hidden');
      accountView.classList.add('hidden');
      siteFooter.classList.add('hidden');
      testView.classList.remove('hidden');

      loadQuestion(currentQIndex);
      startTimer();
    }

    function startTimer() {
      clearInterval(timerInterval);
      const timerDisplay = document.getElementById('timer');

      timerInterval = setInterval(() => {
        timeRemaining--;
        let mins = Math.floor(timeRemaining / 60);
        let secs = timeRemaining % 60;
        timerDisplay.innerText = `${mins}:${secs < 10 ? '0' : ''}${secs}`;

        if (timeRemaining <= 0) {
          clearInterval(timerInterval);
          alert('⏰ Time Up! Submitting Test...');
          submitTest();
        }
      }, 1000);
    }

    function loadQuestion(index) {
      currentQIndex = index;
      testState[index].visited = true;

      const q = activeTest[index];
      document.getElementById('qNumber').innerText = `Question ${index + 1} of ${activeTest.length}`;
      document.getElementById('qText').innerText = q.question;

      const container = document.getElementById('optionsContainer');
      container.innerHTML = '';

      q.options.forEach((optText, optIdx) => {
        const itemDiv = document.createElement('div');
        const isSelected = testState[index].selected === optIdx;
        itemDiv.className = `option-item ${isSelected ? 'selected' : ''}`;
        itemDiv.innerHTML = `<input type="radio" name="optRadio" ${isSelected ? 'checked' : ''}> <span>${optText}</span>`;

        itemDiv.addEventListener('click', () => {
          testState[index].selected = optIdx;
          loadQuestion(index);
        });

        container.appendChild(itemDiv);
      });

      renderPalette();
    }

    function renderPalette() {
      const paletteGrid = document.getElementById('paletteGrid');
      paletteGrid.innerHTML = '';

      activeTest.forEach((_, idx) => {
        const btn = document.createElement('button');
        btn.className = 'p-btn';
        btn.innerText = idx + 1;

        const st = testState[idx];
        if (idx === currentQIndex) btn.classList.add('active-q');

        if (st.reviewed) {
          btn.classList.add('reviewed');
        } else if (st.selected !== null) {
          btn.classList.add('answered');
        } else if (st.visited) {
          btn.classList.add('visited-unanswered');
        }

        btn.addEventListener('click', () => loadQuestion(idx));
        paletteGrid.appendChild(btn);
      });
    }

    function nextQuestion() {
      if (currentQIndex < activeTest.length - 1) {
        loadQuestion(currentQIndex + 1);
      } else {
        alert('You have reached the end of the test. Click "Submit Test 🎯" to see your scorecard!');
      }
    }

    function prevQuestion() {
      if (currentQIndex > 0) {
        loadQuestion(currentQIndex - 1);
      }
    }

    function clearAnswer() {
      testState[currentQIndex].selected = null;
      loadQuestion(currentQIndex);
    }

    function toggleReview() {
      testState[currentQIndex].reviewed = !testState[currentQIndex].reviewed;
      loadQuestion(currentQIndex);
    }

    function submitTest() {
      clearInterval(timerInterval);
      let correctCount = 0;
      let wrongCount = 0;

      activeTest.forEach((q, idx) => {
        const sel = testState[idx].selected;
        if (sel !== null) {
          if (sel === q.correct) correctCount++;
          else wrongCount++;
        }
      });

      let rawScore = Math.max(0, (correctCount * 1) - (wrongCount * 0.33)).toFixed(2);
      let percentage = ((rawScore / activeTest.length) * 100).toFixed(1);
      let attempted = correctCount + wrongCount;
      let accuracy = attempted > 0 ? ((correctCount / attempted) * 100).toFixed(1) : 0;
      let estimatedRank = Math.floor((100 - Math.min(99, Math.max(0, percentage))) * 100 + 15);

      const historyItem = {
        title: activeTestTitle,
        date: new Date().toLocaleString('en-IN', { dateStyle: 'short', timeStyle: 'short' }),
        score: rawScore,
        total: activeTest.length,
        percentage: percentage,
        accuracy: accuracy,
        rank: estimatedRank
      };

      testHistory.push(historyItem);
      localStorage.setItem('rrb_test_history', JSON.stringify(testHistory));

      document.getElementById('resultTestName').innerText = `${activeTestTitle} Performance`;
      document.getElementById('rankDisplay').innerText = `🏆 All India Rank: #${estimatedRank} / 10,000`;
      document.getElementById('resScore').innerText = `${rawScore} / ${activeTest.length}`;
      document.getElementById('resPercentage').innerText = `${percentage}%`;
      document.getElementById('resAccuracy').innerText = `${accuracy}%`;

      testView.classList.add('hidden');
      resultModal.classList.add('active');
    }

    init();
  </script>
</body>
</html>
  <script src="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.5.13/cropper.min.js"></script>

  <style>
    /* --- COLOR PALETTE & GRADIENTS --- */
    :root {
      --bg-color: #f8fafc;
      --bg-mesh: radial-gradient(at 0% 0%, rgba(99, 102, 241, 0.08) 0px, transparent 50%),
                 radial-gradient(at 100% 100%, rgba(245, 158, 11, 0.08) 0px, transparent 50%);
      --card-bg: #ffffff;
      --card-border: rgba(226, 232, 240, 0.8);
      --text-main: #0f172a;
      --text-muted: #64748b;
      --nav-bg: rgba(15, 23, 42, 0.88);
      
      --gradient-primary: linear-gradient(135deg, #6366f1 0%, #a855f7 100%);
      --gradient-primary-hover: linear-gradient(135deg, #4f46e5 0%, #9333ea 100%);
      --gradient-accent: linear-gradient(135deg, #f59e0b 0%, #ef4444 100%);
      --gradient-success: linear-gradient(135deg, #10b981 0%, #059669 100%);
      
      --primary: #6366f1;
      --secondary: #f59e0b;
      --success: #10b981;
      --danger: #ef4444;
      --card-shadow: 0 10px 30px -5px rgba(0, 0, 0, 0.05), 0 4px 6px -2px rgba(0, 0, 0, 0.02);
      --card-hover-shadow: 0 20px 35px -10px rgba(99, 102, 241, 0.18);
      --input-bg: #ffffff;
    }

    body.dark-theme {
      --bg-color: #090d16;
      --bg-mesh: radial-gradient(at 0% 0%, rgba(99, 102, 241, 0.15) 0px, transparent 50%),
                 radial-gradient(at 100% 100%, rgba(168, 85, 247, 0.12) 0px, transparent 50%);
      --card-bg: #131b2e;
      --card-border: rgba(255, 255, 255, 0.08);
      --text-main: #f8fafc;
      --text-muted: #94a3b8;
      --nav-bg: rgba(9, 13, 22, 0.92);
      --card-shadow: 0 15px 35px -5px rgba(0, 0, 0, 0.4);
      --card-hover-shadow: 0 20px 40px -10px rgba(99, 102, 241, 0.3);
      --input-bg: #0d1322;
    }

    * { box-sizing: border-box; margin: 0; padding: 0; font-family: 'Plus Jakarta Sans', sans-serif; transition: background 0.3s cubic-bezier(0.4, 0, 0.2, 1), border-color 0.3s, transform 0.2s; }
    h1, h2, h3, h4, .logo-text, .auth-btn, .card-btn { font-family: 'Outfit', sans-serif; }

    body { background-color: var(--bg-color); background-image: var(--bg-mesh); color: var(--text-main); line-height: 1.6; min-height: 100vh; display: flex; flex-direction: column; overflow-x: hidden; }

    .hidden { display: none !important; }

    /* --- GLASSMORPHISM NAVBAR --- */
    .navbar {
      display: flex; justify-content: space-between; align-items: center; padding: 16px 6%;
      background: var(--nav-bg); backdrop-filter: blur(20px); -webkit-backdrop-filter: blur(20px);
      position: sticky; top: 0; z-index: 1000; border-bottom: 1px solid rgba(255, 255, 255, 0.1);
      box-shadow: 0 10px 30px rgba(0,0,0,0.15);
    }

    .logo-container { display: flex; align-items: center; gap: 10px; cursor: pointer; }

    .logo-text {
      font-size: 24px; font-weight: 800; letter-spacing: -0.5px;
      background: linear-gradient(135deg, #fbbf24 0%, #f59e0b 50%, #ec4899 100%);
      -webkit-background-clip: text; -webkit-text-fill-color: transparent;
      display: flex; align-items: center; gap: 10px;
    }

    .nav-links { display: flex; gap: 24px; align-items: center; }
    .nav-btn { background: none; border: none; font-size: 15px; color: #cbd5e1; font-weight: 600; cursor: pointer; position: relative; padding: 4px 0; }
    .nav-btn:hover { color: #ffffff; }
    .nav-btn::after { content: ''; position: absolute; bottom: 0; left: 0; width: 0; height: 2px; background: var(--gradient-primary); transition: width 0.3s; border-radius: 2px; }
    .nav-btn:hover::after { width: 100%; }

    .theme-toggle-btn {
      background: rgba(255, 255, 255, 0.08); border: 1px solid rgba(255, 255, 255, 0.15); color: #fef08a;
      padding: 8px 16px; border-radius: 30px; cursor: pointer; font-size: 13px; font-weight: 700;
      display: flex; align-items: center; gap: 6px; backdrop-filter: blur(10px);
    }
    .theme-toggle-btn:hover { background: rgba(255, 255, 255, 0.18); transform: translateY(-1px); }

    .auth-btn {
      background: var(--gradient-primary); color: white; border: none; padding: 10px 22px; border-radius: 12px;
      cursor: pointer; font-weight: 700; font-size: 14px; box-shadow: 0 4px 15px rgba(99, 102, 241, 0.35);
    }
    .auth-btn:hover { background: var(--gradient-primary-hover); transform: translateY(-2px); box-shadow: 0 8px 25px rgba(99, 102, 241, 0.5); }

    .user-badge {
      background: rgba(16, 185, 129, 0.12); color: #34d399; border: 1px solid rgba(16, 185, 129, 0.3);
      padding: 6px 16px; border-radius: 30px; font-size: 14px; font-weight: 700; cursor: pointer;
      display: flex; align-items: center; gap: 10px; backdrop-filter: blur(8px);
    }
    .user-badge img { width: 28px; height: 28px; border-radius: 50%; object-fit: cover; border: 2px solid #10b981; }

    /* --- HERO SECTION WITH GLOW & BADGES --- */
    .hero {
      text-align: center; padding: 75px 20px 65px; position: relative; overflow: hidden;
      background: radial-gradient(circle at 50% 0%, rgba(99, 102, 241, 0.25) 0%, rgba(15, 23, 42, 0) 70%);
    }
    .hero-badge {
      display: inline-flex; align-items: center; gap: 8px; background: rgba(99, 102, 241, 0.12);
      border: 1px solid rgba(99, 102, 241, 0.3); color: #818cf8; padding: 6px 18px; border-radius: 30px;
      font-size: 13px; font-weight: 700; margin-bottom: 20px; letter-spacing: 0.5px; text-transform: uppercase;
    }
    .hero h1 { font-size: 46px; font-weight: 800; margin-bottom: 16px; letter-spacing: -1px; line-height: 1.2; }
    .hero h1 span {
      background: linear-gradient(135deg, #38bdf8 0%, #818cf8 50%, #c084fc 100%);
      -webkit-background-clip: text; -webkit-text-fill-color: transparent;
    }
    .hero p { font-size: 17px; color: var(--text-muted); max-width: 680px; margin: 0 auto; font-weight: 500; }

    /* --- CONTAINER & CARDS ENGINE --- */
    .container { max-width: 1240px; margin: 30px auto 60px; padding: 0 24px; flex: 1; width: 100%; }
    
    .section-head { margin-bottom: 28px; display: flex; flex-direction: column; gap: 4px; border-left: 5px solid var(--primary); padding-left: 18px; }
    .section-head h2 { font-size: 26px; font-weight: 800; color: var(--text-main); letter-spacing: -0.5px; }
    .section-head p { color: var(--text-muted); font-size: 14px; font-weight: 500; }

    .grid-3 { display: grid; grid-template-columns: repeat(auto-fit, minmax(340px, 1fr)); gap: 28px; margin-bottom: 60px; }
    .grid-4 { display: grid; grid-template-columns: repeat(auto-fit, minmax(260px, 1fr)); gap: 24px; margin-bottom: 60px; }

    /* MODERN CARD DESIGN */
    .card {
      background: var(--card-bg); padding: 30px; border-radius: 20px; border: 1px solid var(--card-border);
      box-shadow: var(--card-shadow); display: flex; flex-direction: column; justify-content: space-between;
      position: relative; overflow: hidden; backdrop-filter: blur(10px);
    }
    .card::before {
      content: ''; position: absolute; top: 0; left: 0; right: 0; height: 4px;
      background: var(--gradient-primary); opacity: 0.8; transition: height 0.3s;
    }
    .card:hover { transform: translateY(-6px); box-shadow: var(--card-hover-shadow); border-color: rgba(99, 102, 241, 0.4); }
    .card:hover::before { height: 6px; }

    .tag { position: absolute; top: 22px; right: 22px; font-size: 11px; padding: 5px 12px; border-radius: 30px; font-weight: 800; text-transform: uppercase; letter-spacing: 0.5px; }
    .tag-shift { background: rgba(99, 102, 241, 0.15); color: #818cf8; border: 1px solid rgba(99, 102, 241, 0.25); }
    .tag-subject { background: rgba(245, 158, 11, 0.15); color: #fbbf24; border: 1px solid rgba(245, 158, 11, 0.25); }

    .card h3 { font-size: 20px; font-weight: 700; margin-bottom: 10px; padding-right: 80px; color: var(--text-main); }
    .card p { color: var(--text-muted); font-size: 14px; margin-bottom: 24px; font-weight: 400; line-height: 1.5; }

    .btn-group { display: flex; flex-direction: column; gap: 12px; }
    .card-btn {
      width: 100%; padding: 13px; background: var(--gradient-primary); color: white; border: none;
      border-radius: 12px; font-size: 14px; font-weight: 700; cursor: pointer; box-shadow: 0 4px 12px rgba(99, 102, 241, 0.25);
    }
    .card-btn:hover { background: var(--gradient-primary-hover); transform: translateY(-1px); box-shadow: 0 6px 18px rgba(99, 102, 241, 0.4); }

    .pdf-btn {
      width: 100%; padding: 11px; background: transparent; color: var(--secondary); border: 1.5px solid var(--secondary);
      border-radius: 12px; font-size: 13px; font-weight: 700; cursor: pointer; transition: 0.2s;
    }
    .pdf-btn:hover { background: var(--secondary); color: white; transform: translateY(-1px); }

    /* --- DASHBOARD / ACCOUNT & SCORECARDS --- */
    .dashboard-top { display: grid; grid-template-columns: 1fr 1fr; gap: 30px; margin-bottom: 45px; }
    .dash-card {
      background: var(--card-bg); padding: 32px; border-radius: 24px; border: 1px solid var(--card-border);
      box-shadow: var(--card-shadow); backdrop-filter: blur(10px);
    }
    .dash-card h2 { font-size: 22px; font-weight: 800; color: var(--text-main); margin-bottom: 24px; border-bottom: 2px solid var(--card-border); padding-bottom: 14px; }

    /* PROFILE PHOTO UPLOADER (WITH CROP SUPPORT) */
    .pfp-section { display: flex; flex-direction: column; align-items: center; gap: 14px; margin-bottom: 24px; padding-bottom: 20px; border-bottom: 1px dashed var(--card-border); }
    .pfp-preview-box {
      width: 105px; height: 105px; border-radius: 50%; overflow: hidden; border: 3px solid var(--primary);
      box-shadow: 0 8px 20px rgba(99, 102, 241, 0.3); background: var(--card-border); display: flex; justify-content: center; align-items: center;
    }
    .pfp-preview-box img { width: 100%; height: 100%; object-fit: cover; }
    .pfp-upload-btn {
      background: rgba(99, 102, 241, 0.12); color: var(--primary); border: 1.5px dashed var(--primary);
      padding: 9px 18px; border-radius: 10px; font-size: 13px; font-weight: 700; cursor: pointer; text-align: center; transition: 0.2s;
    }
    .pfp-upload-btn:hover { background: var(--primary); color: white; }

    /* CROP CONTAINER */
    .crop-container-box { max-height: 380px; width: 100%; overflow: hidden; margin: 15px 0; background: #000; border-radius: 12px; }
    .crop-container-box img { max-width: 100%; display: block; }

    .info-list { display: flex; flex-direction: column; gap: 12px; margin-bottom: 22px; }
    .info-item { display: flex; justify-content: space-between; padding: 12px 18px; background: rgba(0,0,0,0.03); border-radius: 12px; border: 1px solid var(--card-border); font-size: 14px; }
    .dark-theme .info-item { background: rgba(255,255,255,0.03); }
    .info-item label { font-weight: 600; color: var(--text-muted); }
    .info-item span { font-weight: 700; color: var(--text-main); }

    .stats-box { background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%); color: white; padding: 22px; border-radius: 16px; border: 1px solid rgba(255,255,255,0.1); }
    .stats-box h3 { font-size: 14px; font-weight: 800; color: var(--secondary); margin-bottom: 12px; text-transform: uppercase; letter-spacing: 0.5px; }
    .stats-row { display: flex; justify-content: space-between; font-size: 14px; margin-bottom: 8px; }

    .form-group { margin-bottom: 18px; text-align: left; }
    .form-group label { display: block; font-size: 13px; font-weight: 700; color: var(--text-main); margin-bottom: 8px; }
    .form-group input { width: 100%; padding: 12px; border: 1px solid var(--card-border); background: var(--input-bg); color: var(--text-main); border-radius: 10px; font-size: 14px; outline: none; }
    .form-group input:focus { border-color: var(--primary); box-shadow: 0 0 0 3px rgba(99, 102, 241, 0.2); }

    /* Scorecards Table */
    .history-card { background: var(--card-bg); padding: 32px; border-radius: 24px; border: 1px solid var(--card-border); box-shadow: var(--card-shadow); }
    .table-responsive { overflow-x: auto; margin-top: 18px; }
    table { width: 100%; border-collapse: collapse; text-align: left; font-size: 14px; }
    th { background: rgba(0,0,0,0.04); padding: 14px 18px; color: var(--text-muted); font-weight: 700; border-bottom: 2px solid var(--card-border); }
    .dark-theme th { background: rgba(255,255,255,0.04); }
    td { padding: 16px 18px; border-bottom: 1px solid var(--card-border); color: var(--text-main); font-weight: 500; }

    /* --- TEST INTERFACE VIEW --- */
    .test-container { background: var(--card-bg); min-height: 100vh; display: flex; flex-direction: column; }
    .test-header { background: #0f172a; color: white; padding: 18px 5%; display: flex; justify-content: space-between; align-items: center; border-bottom: 3px solid var(--primary); }
    .timer-box { background: rgba(239, 68, 68, 0.15); border: 1px solid rgba(239, 68, 68, 0.4); color: #f87171; padding: 8px 20px; border-radius: 10px; font-weight: 800; font-size: 20px; letter-spacing: 1px; }

    .test-body { display: flex; flex: 1; }
    .question-section { flex: 3; padding: 45px; border-right: 1px solid var(--card-border); }
    .palette-section { flex: 1; padding: 35px; background: rgba(0,0,0,0.02); }

    .q-number { font-weight: 800; color: var(--primary); font-size: 14px; text-transform: uppercase; margin-bottom: 10px; letter-spacing: 0.5px; }
    .q-text { font-size: 19px; font-weight: 600; color: var(--text-main); margin-bottom: 30px; line-height: 1.6; }

    .options-list { display: flex; flex-direction: column; gap: 14px; }
    .option-item { background: var(--card-bg); padding: 16px 22px; border-radius: 12px; border: 2px solid var(--card-border); cursor: pointer; display: flex; align-items: center; gap: 14px; font-size: 15px; font-weight: 500; color: var(--text-main); }
    .option-item:hover { border-color: rgba(99, 102, 241, 0.5); }
    .option-item.selected { border-color: var(--primary); background: rgba(99, 102, 241, 0.1); color: var(--primary); font-weight: 700; }

    .test-footer { padding: 20px 5%; background: var(--card-bg); border-top: 1px solid var(--card-border); display: flex; justify-content: space-between; align-items: center; }
    .action-btn { padding: 12px 26px; border-radius: 10px; border: none; font-weight: 700; font-size: 14px; cursor: pointer; }
    .btn-next { background: var(--gradient-primary); color: white; box-shadow: 0 4px 12px rgba(99, 102, 241, 0.3); }
    .btn-clear { background: rgba(0,0,0,0.06); color: var(--text-muted); }
    .dark-theme .btn-clear { background: rgba(255,255,255,0.08); }
    .btn-submit { background: var(--gradient-success); color: white; box-shadow: 0 4px 12px rgba(16, 185, 129, 0.3); }

    .palette-grid { display: grid; grid-template-columns: repeat(5, 1fr); gap: 10px; margin-top: 20px; max-height: 420px; overflow-y: auto; }
    .p-btn { width: 38px; height: 38px; border-radius: 8px; border: 1px solid var(--card-border); background: var(--card-bg); color: var(--text-main); font-weight: 700; cursor: pointer; font-size: 13px; }
    .p-btn.answered { background: var(--success); color: white; border-color: var(--success); }
    .p-btn.unanswered { background: var(--danger); color: white; border-color: var(--danger); }

    /* --- MODALS GLASS ENGINE --- */
    .modal-overlay { position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(15, 23, 42, 0.75); backdrop-filter: blur(6px); display: none; justify-content: center; align-items: center; z-index: 2000; }
    .modal-overlay.active { display: flex; }
    .modal-box { background: var(--card-bg); padding: 35px; border-radius: 24px; width: 90%; max-width: 490px; position: relative; text-align: center; color: var(--text-main); border: 1px solid var(--card-border); box-shadow: var(--card-shadow); }
    .close-btn { position: absolute; top: 18px; right: 22px; font-size: 26px; cursor: pointer; background: none; border: none; color: var(--text-muted); }

    /* --- FOOTER --- */
    .site-footer {
      background: var(--nav-bg); color: #94a3b8; text-align: center; padding: 25px 20px;
      font-size: 14px; font-weight: 500; border-top: 1px solid rgba(255, 255, 255, 0.1); margin-top: auto;
    }
    .site-footer strong { color: var(--secondary); font-weight: 700; }

    @media(max-width: 850px) {
      .dashboard-top { grid-template-columns: 1fr; }
      .test-body { flex-direction: column; }
      .question-section { border-right: none; padding: 24px; }
      .hero h1 { font-size: 32px; }
    }
  </style>
</head>
<body>

  <!-- Glassmorphism Navbar -->
  <header class="navbar">
    <div class="logo-container" id="navLogo">
      <div class="logo-text">🚆 Crack RRB JE <span class="auto-year">2026</span></div>
    </div>
    <nav class="nav-links">
      <button class="nav-btn" id="navHome">Home</button>
      <button class="nav-btn" id="navAccount">Account & Settings</button>
      <button class="theme-toggle-btn" id="themeToggleBtn">🌙 Dark Mode</button>
    </nav>
    <div id="authContainer">
      <button id="loginNavBtn" class="auth-btn">Login</button>
      <div id="userBadge" class="user-badge hidden">
        <img id="navPfpImg" src="https://cdn-icons-png.flaticon.com/512/847/847969.png" alt="Profile">
        <span id="navUsernameText">Profile</span>
      </div>
    </div>
  </header>

  <!-- MAIN HOME VIEW -->
  <main id="homeView">
    <section class="hero">
      <div class="hero-badge">⚡ Official Railway CBT Exam Portal</div>
      <h1>Crack <span>RRB JE <span class="auto-year">2026</span></span> With Real Shift Mocks</h1>
      <p>Attempt 90-Question Official Shift Mocks (90 Mins) & Subject Tests with Instant PDF Downloads.</p>
    </section>

    <div class="container">
      <div class="section-head">
        <h2>🚆 Official Shift-Wise Full Mock Tests (90 Qs - 90 Mins)</h2>
        <p>Complete CBT-1 Exam Pattern Mocks with Timer & Official Question Paper Download</p>
      </div>

      <div class="grid-3">
        <div class="card">
          <span class="tag tag-shift">Shift 1 Mock</span>
          <div>
            <h3>⚡ Shift-1 Full Mock Test</h3>
            <p>90 Questions covering Technical, Maths, Reasoning & Science as per Shift-1 Standard.</p>
          </div>
          <div class="btn-group">
            <button class="card-btn start-test-btn" data-type="shift1">Start Shift-1 Mock 🚀</button>
            <button class="pdf-btn download-pdf-btn" data-shift="Shift-1">📥 Download Shift-1 Paper PDF</button>
          </div>
        </div>

        <div class="card">
          <span class="tag tag-shift">Shift 2 Mock</span>
          <div>
            <h3>⚡ Shift-2 Full Mock Test</h3>
            <p>90 Questions moderated difficulty paper based on TCS Exam Patterns.</p>
          </div>
          <div class="btn-group">
            <button class="card-btn start-test-btn" data-type="shift2">Start Shift-2 Mock 🚀</button>
            <button class="pdf-btn download-pdf-btn" data-shift="Shift-2">📥 Download Shift-2 Paper PDF</button>
          </div>
        </div>

        <div class="card">
          <span class="tag tag-shift">Shift 3 Mock</span>
          <div>
            <h3>⚡ Shift-3 High Yield Mock</h3>
            <p>90 Advanced practice questions set for maximizing exam speed and score.</p>
          </div>
          <div class="btn-group">
            <button class="card-btn start-test-btn" data-type="shift3">Start Shift-3 Mock 🚀</button>
            <button class="pdf-btn download-pdf-btn" data-shift="Shift-3">📥 Download Shift-3 Paper PDF</button>
          </div>
        </div>
      </div>

      <div class="section-head">
        <h2>📚 Subject-Wise Speed Practice (20 Questions Each)</h2>
        <p>Boost your individual subject accuracy with 20-minute timed tests</p>
      </div>

      <div class="grid-4">
        <div class="card">
          <span class="tag tag-subject">20 Questions</span>
          <div>
            <h3>📐 Mathematics</h3>
            <p>Algebra, Trigonometry, SI-CI, Time & Work Speed Test.</p>
          </div>
          <button class="card-btn start-test-btn" data-type="maths">Attempt Maths Test</button>
        </div>

        <div class="card">
          <span class="tag tag-subject">20 Questions</span>
          <div>
            <h3>🧠 Reasoning</h3>
            <p>Coding-Decoding, Series, Syllogism & Analogy Practice.</p>
          </div>
          <button class="card-btn start-test-btn" data-type="reasoning">Attempt Reasoning Test</button>
        </div>

        <div class="card">
          <span class="tag tag-subject">20 Questions</span>
          <div>
            <h3>🔬 General Science</h3>
            <p>Physics, Chemistry & Life Sciences High Yield Questions.</p>
          </div>
          <button class="card-btn start-test-btn" data-type="science">Attempt Science Test</button>
        </div>

        <div class="card">
          <span class="tag tag-subject">20 Questions</span>
          <div>
            <h3>🌍 General Awareness</h3>
            <p>Railways History, Current Affairs & Polity Quick Test.</p>
          </div>
          <button class="card-btn start-test-btn" data-type="gk">Attempt GA Test</button>
        </div>
      </div>
    </div>
  </main>

  <!-- ACCOUNT & PRIVACY SETTINGS + SCORECARDS VIEW -->
  <section id="accountView" class="container hidden">
    <div class="section-head">
      <h2>⚙️ Account & Privacy Settings</h2>
      <p>Manage profile credentials, uploaded profile photo, live IP stats & attempt history.</p>
    </div>

    <div class="dashboard-top">
      <div class="dash-card">
        <h2>🔒 Account Details & Profile Picture</h2>
        
        <!-- PROFILE PHOTO UPLOADER (WITH CROP SUPPORT) -->
        <div class="pfp-section">
          <div class="pfp-preview-box">
            <img id="accPfpPreview" src="https://cdn-icons-png.flaticon.com/512/847/847969.png" alt="User PFP">
          </div>
          <label for="pfpFileInput" class="pfp-upload-btn">📷 Select Photo to Crop & Set Profile</label>
          <input type="file" id="pfpFileInput" accept="image/*" class="hidden">
        </div>

        <form id="updateAccountForm">
          <div class="form-group">
            <label>Username / Name</label>
            <input type="text" id="accUsername" required>
          </div>
          <div class="form-group">
            <label>Phone Number</label>
            <input type="tel" id="accPhone" required>
          </div>
          <div class="form-group">
            <label>Password (Change Privacy Password)</label>
            <input type="password" id="accPassword" placeholder="Enter new password">
          </div>
          <button type="submit" class="auth-btn" style="width: 100%; padding: 12px;">Save Changes 💾</button>
        </form>
        <button id="logoutBtn" style="width:100%; padding:11px; margin-top:15px; background:#ef4444; color:white; border:none; border-radius:12px; font-weight:700; cursor:pointer;">Logout Account</button>
      </div>

      <div class="dash-card">
        <h2>👨‍💻 About Me (Owner Profile)</h2>
        <div class="info-list">
          <div class="info-item"><label>Name:</label><span>Amandeep</span></div>
          <div class="info-item"><label>Email ID:</label><span>amandeep83403@gmail.com</span></div>
          <div class="info-item"><label>Number:</label><span>95182 90278</span></div>
          <div class="info-item"><label>Age:</label><span>22 Years</span></div>
          <div class="info-item"><label>Address:</label><span>Kelnian, Sirsa (Haryana)</span></div>
        </div>

        <div class="stats-box">
          <h3>🌐 Live Portal Visitor Analytics</h3>
          <div class="stats-row"><span>Site Uptime:</span><strong id="sessionUptime" style="color:#4ade80;">00:00:00</strong></div>
          <div class="stats-row"><span>Unique IP Visits:</span><strong id="uniqueVisitors" style="color:#fbbf24;">1 Visit</strong></div>
        </div>
      </div>
    </div>

    <div class="history-card">
      <h2 style="font-size: 22px; font-weight: 800; border-bottom: 2px solid var(--card-border); padding-bottom: 14px; margin-bottom: 15px;">
        📊 My Attempted Mock Tests & Performance Scorecards
      </h2>
      <div class="table-responsive">
        <table>
          <thead>
            <tr>
              <th>Test Name</th>
              <th>Date & Time</th>
              <th>Score</th>
              <th>Percentage %</th>
              <th>Accuracy %</th>
              <th>All India Rank</th>
            </tr>
          </thead>
          <tbody id="scorecardTableBody">
            <tr>
              <td colspan="6" style="text-align: center; color: var(--text-muted); padding: 20px;">No mock tests attempted yet. Start a test to view performance here!</td>
            </tr>
          </tbody>
        </table>
      </div>
    </div>
  </section>

  <!-- TEST INTERFACE VIEW -->
  <section id="testView" class="test-container hidden">
    <div class="test-header">
      <div>
        <h3 id="testTitleDisplay" style="font-size: 18px; font-weight: 700;">Crack RRB JE - Mock Test</h3>
        <small style="color: #94a3b8;">Correct: +1.0 | Negative: -0.33</small>
      </div>
      <div class="timer-box">⏱️ <span id="timer">90:00</span></div>
    </div>

    <div class="test-body">
      <div class="question-section">
        <div class="q-number" id="qNumber">Question 1</div>
        <div class="q-text" id="qText">Loading question...</div>
        <div class="options-list" id="optionsContainer"></div>
      </div>

      <div class="palette-section">
        <h4 style="font-size: 14px; font-weight: 800; color: var(--text-main);">Question Palette</h4>
        <div class="palette-grid" id="paletteGrid"></div>
      </div>
    </div>

    <div class="test-footer">
      <button class="action-btn btn-clear" id="clearAnswerBtn">Clear Selection</button>
      <div>
        <button class="action-btn btn-next" id="nextQuestionBtn">Save & Next ➡️</button>
        <button class="action-btn btn-submit" id="submitTestBtn">Submit Test 🎯</button>
      </div>
    </div>
  </section>

  <!-- CROPPER POPUP MODAL -->
  <div class="modal-overlay" id="cropModal">
    <div class="modal-box">
      <button class="close-btn" id="closeCropModalBtn">&times;</button>
      <h3 style="font-size: 20px; font-weight: 800; margin-bottom: 8px;">✂️ Adjust & Crop Profile Picture</h3>
      <p style="font-size: 13px; color: var(--text-muted);">Crop area adjust karke 'Save Profile Picture' button par click karein.</p>
      
      <div class="crop-container-box">
        <img id="cropImageTarget" src="" alt="Crop Target">
      </div>

      <button id="saveCroppedPfpBtn" class="auth-btn" style="width: 100%; padding: 12px;">Crop & Save Profile Picture 📸</button>
    </div>
  </div>

  <!-- RESULT MODAL -->
  <div class="modal-overlay" id="resultModal">
    <div class="modal-box" style="max-width: 550px;">
      <h2 style="font-size: 24px; font-weight: 800;">🎯 Official Performance Scorecard</h2>
      <p style="color: var(--text-muted); font-size: 14px; margin-bottom: 20px;" id="resultTestName">RRB JE Shift Test</p>

      <div style="background: linear-gradient(135deg, #f59e0b, #d97706); color: white; padding: 12px 24px; border-radius: 30px; display: inline-block; font-weight: 800; font-size: 18px; margin-bottom: 20px; box-shadow: 0 4px 15px rgba(245, 158, 11, 0.4);" id="rankDisplay">
        🥇 All India Rank: #142 / 10,000
      </div>

      <div style="display: grid; grid-template-columns: repeat(3, 1fr); gap: 12px; margin-bottom: 20px;">
        <div style="background:rgba(0,0,0,0.04); padding:14px; border-radius:12px; border:1px solid var(--card-border);">
          <small style="color:var(--text-muted);">Score</small>
          <p id="resScore" style="font-weight:800; font-size:18px; color:var(--primary);">0.00</p>
        </div>
        <div style="background:rgba(0,0,0,0.04); padding:14px; border-radius:12px; border:1px solid var(--card-border);">
          <small style="color:var(--text-muted);">Percentage</small>
          <p id="resPercentage" style="font-weight:800; font-size:18px; color:var(--primary);">0%</p>
        </div>
        <div style="background:rgba(0,0,0,0.04); padding:14px; border-radius:12px; border:1px solid var(--card-border);">
          <small style="color:var(--text-muted);">Accuracy</small>
          <p id="resAccuracy" style="font-weight:800; font-size:18px; color:var(--primary);">0%</p>
        </div>
      </div>

      <button id="closeResultBtn" class="auth-btn" style="width: 100%; padding: 12px;">Go to Account & Scorecards</button>
    </div>
  </div>

  <!-- LOGIN MODAL -->
  <div class="modal-overlay" id="loginModal">
    <div class="modal-box">
      <button class="close-btn" id="closeModalBtn">&times;</button>
      <h2 style="font-size: 22px; font-weight: 800; margin-bottom: 6px;">Aspirant Login 🔒</h2>
      <p style="font-size: 13px; color: var(--text-muted); margin-bottom: 20px;">Test attempt karne ke liye login karein.</p>

      <form id="loginForm">
        <div class="form-group">
          <label>Username / Name</label>
          <input type="text" id="loginUsername" placeholder="e.g. Amandeep" required>
        </div>
        <div class="form-group">
          <label>Mobile Number</label>
          <input type="tel" id="loginPhone" placeholder="Enter Mobile Number" required>
        </div>
        <button type="submit" class="auth-btn" style="width: 100%; margin-top: 10px;">Login & Continue</button>
      </form>
    </div>
  </div>

  <!-- FOOTER SECTION -->
  <footer class="site-footer" id="siteFooter">
    ⚡ Crack RRB JE <span class="auto-year">2026</span> Full Shift Mocks (90 Qs) & Subject Practice Live! &nbsp;|&nbsp; Made by <strong>Amandeep</strong>
  </footer>

  <!-- JAVASCRIPT ENGINE -->
  <script>
    // AUTOMATIC YEAR UPDATE
    const CURRENT_YEAR = new Date().getFullYear();
    document.querySelectorAll('.auto-year').forEach(el => el.innerText = CURRENT_YEAR);

    const DEFAULT_PFP = "https://cdn-icons-png.flaticon.com/512/847/847969.png";

    // GENERATE 90 QUESTIONS FOR SHIFT MOCKS & 20 QUESTIONS FOR SUBJECT TESTS
    function generateShiftQuestions(shiftName) {
      return Array.from({length: 90}, (_, i) => ({
        id: i + 1,
        question: `RRB JE ${shiftName} Official PYQ Q${i+1} Standard Question based on Technical, Mathematics, General Science & Reasoning topics #${i+1}?`,
        options: [`Option A for Q${i+1}`, `Option B for Q${i+1}`, `Option C for Q${i+1}`, `Option D for Q${i+1}`],
        correct: (i % 4)
      }));
    }

    const questionBank = {
      maths: Array.from({length: 20}, (_, i) => ({ id: i+1, question: `Maths Q${i+1} Solve equation: ${i+2}x + 10 = ${30 + i*2}`, options: ["x = 10", "x = 5", "x = 8", "x = 12"], correct: 0 })),
      reasoning: Array.from({length: 20}, (_, i) => ({ id: i+1, question: `Reasoning Q${i+1} Find missing term in series: ${i*2}, ${i*2 + 4}, ${i*2 + 8}, ?`, options: [`${i*2 + 12}`, `${i*2 + 10}`, `${i*2 + 14}`, `${i*2 + 16}`], correct: 0 })),
      science: Array.from({length: 20}, (_, i) => ({ id: i+1, question: `General Science Q${i+1} What is the SI unit of physical quantity #${i+1}?`, options: ["Joule", "Pascal", "Newton", "Watt"], correct: (i % 4) })),
      gk: Array.from({length: 20}, (_, i) => ({ id: i+1, question: `General Awareness Q${i+1} Railway Zone #${i+1} headquarters is situated in?`, options: ["New Delhi", "Mumbai", "Kolkata", "Chennai"], correct: (i % 4) })),
      
      shift1: generateShiftQuestions("Shift-1"),
      shift2: generateShiftQuestions("Shift-2"),
      shift3: generateShiftQuestions("Shift-3")
    };

    // State Variables (userData uses sessionStorage so refresh keeps login, tab close logs out)
    let userData = JSON.parse(sessionStorage.getItem('rrb_user')) || null;
    let testHistory = JSON.parse(localStorage.getItem('rrb_test_history')) || [];
    let activeTest = [];
    let activeTestTitle = "";
    let currentQIndex = 0;
    let userAnswers = {};
    let timerInterval = null;
    let timeRemaining = 5400;
    let startTime = Date.now();

    // Cropper instance
    let cropperInstance = null;

    // DOM Elements
    const homeView = document.getElementById('homeView');
    const accountView = document.getElementById('accountView');
    const testView = document.getElementById('testView');
    const loginModal = document.getElementById('loginModal');
    const resultModal = document.getElementById('resultModal');
    const cropModal = document.getElementById('cropModal');
    const themeToggleBtn = document.getElementById('themeToggleBtn');
    const siteFooter = document.getElementById('siteFooter');
    const pfpFileInput = document.getElementById('pfpFileInput');
    const cropImageTarget = document.getElementById('cropImageTarget');

    function init() {
      updateAuthUI();
      startSessionTimer();
      trackUniqueVisitorByIP();
      renderScorecardHistory();
      initTheme();

      themeToggleBtn.addEventListener('click', toggleTheme);

      // Navigations
      document.getElementById('navHome').addEventListener('click', showHome);
      document.getElementById('navLogo').addEventListener('click', showHome);
      document.getElementById('navAccount').addEventListener('click', showAccountView);
      document.getElementById('userBadge').addEventListener('click', showAccountView);

      // Auth
      document.getElementById('loginNavBtn').addEventListener('click', () => loginModal.classList.add('active'));
      document.getElementById('closeModalBtn').addEventListener('click', () => loginModal.classList.remove('active'));
      document.getElementById('loginForm').addEventListener('submit', handleLogin);
      document.getElementById('updateAccountForm').addEventListener('submit', handleAccountUpdate);
      document.getElementById('logoutBtn').addEventListener('click', handleLogout);

      // CROPPER PFP EVENTS
      pfpFileInput.addEventListener('change', handlePfpSelectForCrop);
      document.getElementById('saveCroppedPfpBtn').addEventListener('click', handleSaveCroppedPfp);
      document.getElementById('closeCropModalBtn').addEventListener('click', closeCropModal);

      // Test Start Buttons
      document.querySelectorAll('.start-test-btn').forEach(btn => {
        btn.addEventListener('click', () => {
          const testType = btn.getAttribute('data-type');
          if (!userData) loginModal.classList.add('active');
          else initiateTest(testType);
        });
      });

      // PDF Downloads
      document.querySelectorAll('.download-pdf-btn').forEach(btn => {
        btn.addEventListener('click', (e) => {
          const shift = e.target.getAttribute('data-shift');
          downloadRealPDF(shift);
        });
      });

      // Test Controller
      document.getElementById('nextQuestionBtn').addEventListener('click', nextQuestion);
      document.getElementById('clearAnswerBtn').addEventListener('click', clearAnswer);
      document.getElementById('submitTestBtn').addEventListener('click', submitTest);
      document.getElementById('closeResultBtn').addEventListener('click', () => {
        resultModal.classList.remove('active');
        showAccountView();
      });
    }

    // CROPPER ENGINE
    function handlePfpSelectForCrop(e) {
      const file = e.target.files[0];
      if (file) {
        const reader = new FileReader();
        reader.onload = function(event) {
          cropImageTarget.src = event.target.result;
          cropModal.classList.add('active');

          if (cropperInstance) cropperInstance.destroy();

          cropperInstance = new Cropper(cropImageTarget, {
            aspectRatio: 1,
            viewMode: 1,
            autoCropArea: 0.85,
            background: false
          });
        };
        reader.readAsDataURL(file);
      }
      e.target.value = '';
    }

    function handleSaveCroppedPfp() {
      if (!cropperInstance) return;

      const canvas = cropperInstance.getCroppedCanvas({ width: 300, height: 300 });

      if (canvas) {
        const croppedBase64 = canvas.toDataURL('image/png');
        userData.pfp = croppedBase64;
        sessionStorage.setItem('rrb_user', JSON.stringify(userData));

        document.getElementById('accPfpPreview').src = croppedBase64;
        document.getElementById('navPfpImg').src = croppedBase64;

        closeCropModal();
        alert('✂️ Profile Picture successfully cropped and updated!');
      }
    }

    function closeCropModal() {
      if (cropperInstance) {
        cropperInstance.destroy();
        cropperInstance = null;
      }
      cropModal.classList.remove('active');
    }

    // THEME ENGINE
    function initTheme() {
      const isDark = localStorage.getItem('rrb_theme') === 'dark';
      if (isDark) {
        document.body.classList.add('dark-theme');
        themeToggleBtn.innerText = '☀️ Light Mode';
      } else {
        document.body.classList.remove('dark-theme');
        themeToggleBtn.innerText = '🌙 Dark Mode';
      }
    }

    function toggleTheme() {
      document.body.classList.toggle('dark-theme');
      const isDark = document.body.classList.contains('dark-theme');
      localStorage.setItem('rrb_theme', isDark ? 'dark' : 'light');
      themeToggleBtn.innerText = isDark ? '☀️ Light Mode' : '🌙 Dark Mode';
    }

    // PDF DOWNLOAD
    function downloadRealPDF(shiftName) {
      const qList = questionBank[shiftName.toLowerCase().replace('-', '')] || questionBank['shift1'];
      
      let pdfContent = `=================================================================\n`;
      pdfContent += `         CRACK RRB JE EXAM ${CURRENT_YEAR} - OFFICIAL QUESTION PAPER     \n`;
      pdfContent += `=================================================================\n`;
      pdfContent += `Shift: ${shiftName} | Total Questions: ${qList.length} | Time: ${qList.length === 90 ? '90 Minutes' : '20 Minutes'}\n`;
      pdfContent += `=================================================================\n\n`;

      qList.forEach((q, idx) => {
        pdfContent += `Q${idx+1}: ${q.question}\n`;
        pdfContent += `   [A] ${q.options[0]}\n`;
        pdfContent += `   [B] ${q.options[1]}\n`;
        pdfContent += `   [C] ${q.options[2]}\n`;
        pdfContent += `   [D] ${q.options[3]}\n`;
        pdfContent += `   --> Correct Option Key: Option ${String.fromCharCode(65 + q.correct)}\n\n`;
      });

      const blob = new Blob([pdfContent], { type: 'text/plain;charset=utf-8' });
      const link = document.createElement('a');
      link.href = URL.createObjectURL(blob);
      link.download = `RRB_JE_${shiftName}_Official_Question_Paper.txt`;
      document.body.appendChild(link);
      link.click();
      document.body.removeChild(link);
    }

    // NAVIGATION
    function showHome() {
      homeView.classList.remove('hidden');
      accountView.classList.add('hidden');
      testView.classList.add('hidden');
      siteFooter.classList.remove('hidden');
    }

    function showAccountView() {
      if (!userData) return loginModal.classList.add('active');
      homeView.classList.add('hidden');
      testView.classList.add('hidden');
      accountView.classList.remove('hidden');
      siteFooter.classList.remove('hidden');

      document.getElementById('accUsername').value = userData.username;
      document.getElementById('accPhone').value = userData.phone;
      document.getElementById('accPassword').value = userData.password || '123456';
      document.getElementById('accPfpPreview').src = userData.pfp || DEFAULT_PFP;

      renderScorecardHistory();
    }

    function updateAuthUI() {
      const userBadge = document.getElementById('userBadge');
      const loginNavBtn = document.getElementById('loginNavBtn');
      if (userData) {
        loginNavBtn.classList.add('hidden');
        userBadge.classList.remove('hidden');
        document.getElementById('navUsernameText').innerText = userData.username;
        document.getElementById('navPfpImg').src = userData.pfp || DEFAULT_PFP;
      } else {
        loginNavBtn.classList.remove('hidden');
        userBadge.classList.add('hidden');
      }
    }

    function handleLogin(e) {
      e.preventDefault();
      const username = document.getElementById('loginUsername').value;
      const phone = document.getElementById('loginPhone').value;
      userData = { username, phone, password: 'password123', pfp: DEFAULT_PFP };
      sessionStorage.setItem('rrb_user', JSON.stringify(userData));
      updateAuthUI();
      loginModal.classList.remove('active');
    }

    function handleAccountUpdate(e) {
      e.preventDefault();
      userData.username = document.getElementById('accUsername').value;
      userData.phone = document.getElementById('accPhone').value;
      userData.password = document.getElementById('accPassword').value;
      sessionStorage.setItem('rrb_user', JSON.stringify(userData));
      updateAuthUI();
      alert('🔒 Account Credentials Saved Successfully!');
    }

    function handleLogout() {
      userData = null;
      sessionStorage.removeItem('rrb_user');
      updateAuthUI();
      showHome();
    }

    // SCORECARD HISTORY
    function renderScorecardHistory() {
      const tbody = document.getElementById('scorecardTableBody');
      if (testHistory.length === 0) {
        tbody.innerHTML = `<tr><td colspan="6" style="text-align: center; color: var(--text-muted); padding: 20px;">No mock tests attempted yet. Start a test to view performance here!</td></tr>`;
        return;
      }

      tbody.innerHTML = '';
      testHistory.slice().reverse().forEach(item => {
        const tr = document.createElement('tr');
        tr.innerHTML = `
          <td><strong>${item.title}</strong></td>
          <td>${item.date}</td>
          <td style="color:var(--primary); font-weight:700;">${item.score} / ${item.total}</td>
          <td>${item.percentage}%</td>
          <td>${item.accuracy}%</td>
          <td style="color:var(--secondary); font-weight:700;">#${item.rank}</td>
        `;
        tbody.appendChild(tr);
      });
    }

    // ANALYTICS
    function startSessionTimer() {
      setInterval(() => {
        let diff = Math.floor((Date.now() - startTime) / 1000);
        let hrs = Math.floor(diff / 3600);
        let mins = Math.floor((diff % 3600) / 60);
        let secs = diff % 60;
        document.getElementById('sessionUptime').innerText = `${hrs<10?'0':''}${hrs}:${mins<10?'0':''}${mins}:${secs<10?'0':''}${secs}`;
      }, 1000);
    }

    async function trackUniqueVisitorByIP() {
      let visitedIPs = JSON.parse(localStorage.getItem('rrb_visited_ips')) || [];
      try {
        const response = await fetch('https://api.ipify.org?format=json');
        const data = await response.json();
        if (!visitedIPs.includes(data.ip)) {
          visitedIPs.push(data.ip);
          localStorage.setItem('rrb_visited_ips', JSON.stringify(visitedIPs));
        }
        document.getElementById('uniqueVisitors').innerText = `${visitedIPs.length} Unique Visitor(s)`;
      } catch (err) {
        if(visitedIPs.length === 0) visitedIPs.push("127.0.0.1");
        document.getElementById('uniqueVisitors').innerText = `${visitedIPs.length} Unique Visitor(s)`;
      }
    }

    // TEST ENGINE
    function initiateTest(type) {
      activeTest = questionBank[type] || questionBank['shift1'];
      activeTestTitle = type.toUpperCase() + " Mock Test";
      document.getElementById('testTitleDisplay').innerText = `Crack RRB JE - ${activeTestTitle}`;
      
      currentQIndex = 0;
      userAnswers = {};
      timeRemaining = activeTest.length === 90 ? 5400 : 1200;

      homeView.classList.add('hidden');
      accountView.classList.add('hidden');
      siteFooter.classList.add('hidden');
      testView.classList.remove('hidden');

      renderPalette();
      loadQuestion(currentQIndex);
      startTimer();
    }

    function startTimer() {
      clearInterval(timerInterval);
      const timerDisplay = document.getElementById('timer');

      timerInterval = setInterval(() => {
        timeRemaining--;
        let mins = Math.floor(timeRemaining / 60);
        let secs = timeRemaining % 60;
        timerDisplay.innerText = `${mins}:${secs < 10 ? '0' : ''}${secs}`;

        if (timeRemaining <= 0) {
          clearInterval(timerInterval);
          alert('Time Up! Submitting Test...');
          submitTest();
        }
      }, 1000);
    }

    function renderPalette() {
      const paletteGrid = document.getElementById('paletteGrid');
      paletteGrid.innerHTML = '';

      activeTest.forEach((_, idx) => {
        const btn = document.createElement('button');
        btn.className = 'p-btn';
        btn.innerText = idx + 1;

        if (userAnswers[idx] !== undefined) btn.classList.add('answered');
        else if (idx === currentQIndex) btn.classList.add('unanswered');

        btn.addEventListener('click', () => {
          currentQIndex = idx;
          loadQuestion(currentQIndex);
        });
        paletteGrid.appendChild(btn);
      });
    }

    function loadQuestion(index) {
      const q = activeTest[index];
      document.getElementById('qNumber').innerText = `Question ${index + 1} of ${activeTest.length}`;
      document.getElementById('qText').innerText = q.question;

      const container = document.getElementById('optionsContainer');
      container.innerHTML = '';

      q.options.forEach((opt, optIdx) => {
        const label = document.createElement('label');
        const isSelected = userAnswers[index] === optIdx;
        label.className = `option-item ${isSelected ? 'selected' : ''}`;

        label.innerHTML = `<input type="radio" name="option" value="${optIdx}" ${isSelected ? 'checked' : ''}> <span>${opt}</span>`;
        label.addEventListener('click', () => {
          userAnswers[index] = optIdx;
          loadQuestion(index);
        });
        container.appendChild(label);
      });
      renderPalette();
    }

    function nextQuestion() {
      if (currentQIndex < activeTest.length - 1) {
        currentQIndex++;
        loadQuestion(currentQIndex);
      } else {
        alert('Last question reached. Click "Submit Test" when ready!');
      }
    }

    function clearAnswer() {
      delete userAnswers[currentQIndex];
      loadQuestion(currentQIndex);
    }

    function submitTest() {
      clearInterval(timerInterval);
      let correctCount = 0;
      let wrongCount = 0;

      activeTest.forEach((q, idx) => {
        const ans = userAnswers[idx];
        if (ans !== undefined) {
          if (ans === q.correct) correctCount++;
          else wrongCount++;
        }
      });

      let rawScore = Math.max(0, (correctCount * 1) - (wrongCount * 0.33)).toFixed(2);
      let percentage = ((rawScore / activeTest.length) * 100).toFixed(1);
      let attempted = correctCount + wrongCount;
      let accuracy = attempted > 0 ? ((correctCount / attempted) * 100).toFixed(1) : 0;
      let estimatedRank = Math.floor((100 - Math.min(99, percentage)) * 100 + 12);

      const historyItem = {
        title: activeTestTitle,
        date: new Date().toLocaleString('en-IN', { dateStyle: 'short', timeStyle: 'short' }),
        score: rawScore,
        total: activeTest.length,
        percentage: percentage,
        accuracy: accuracy,
        rank: estimatedRank
      };

      testHistory.push(historyItem);
      localStorage.setItem('rrb_test_history', JSON.stringify(testHistory));

      document.getElementById('resultTestName').innerText = `${activeTestTitle} Performance`;
      document.getElementById('rankDisplay').innerText = `🏆 All India Rank: #${estimatedRank} / 10,000`;
      document.getElementById('resScore').innerText = `${rawScore} / ${activeTest.length}`;
      document.getElementById('resPercentage').innerText = `${percentage}%`;
      document.getElementById('resAccuracy').innerText = `${accuracy}%`;

      testView.classList.add('hidden');
      resultModal.classList.add('active');
    }

    init();
  </script>
</body>
</html>
